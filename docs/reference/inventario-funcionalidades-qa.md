# Inventario global de funcionalidades — Codetuver Avatar (para QA)

> Construido leyendo función por función los 55 archivos `.ts`/`.vue` de `src/` y los 11 módulos de `src-tauri/src/` (excluyendo `.spec.ts`) — cada símbolo exportado, cada `#[tauri::command]`, cada función/prop/emit de componente Vue. Organizado por Epic/Feature (misma jerarquía de `flujo_projects/codetuver-avatar/03_requerimientos.md`) para que cada bloque sea una unidad de prueba con sentido de negocio, no un dump de nombres de función.
>
> **Criterio de "Revisado" (nivel perfecto):** solo se marca ✅ si se probó en la app real (no harness), con una sesión real cuando aplica, con evidencia concreta (captura, dato leído en vivo, o causa raíz confirmada en código) — no "se vio de pasada" ni "debería funcionar porque el código se ve bien". Todo lo demás queda ⬜, sin excepción, aunque parezca trivial.
>
> Estado global (actualizado 2026-09-04, continuación de la reapertura de QA): **78 ítems verificables listados (etiquetados `[QA-001]`..`[QA-078]`), 60 están ✅ revisados a nivel perfecto** (11 tras revalidar el Hallazgo 10 desde 5 ángulos y reabrir el barrido, + 14 más en esta continuación: `[QA-003]`, `[QA-008]`, `[QA-013]`, `[QA-014]`, `[QA-018]`, `[QA-027]`, `[QA-029]`, `[QA-035]`, `[QA-040]`, `[QA-046]`, `[QA-055]`, `[QA-065]`, `[QA-067]`, `[QA-076]`). El resto (18) es alcance real pendiente por razones ya declaradas por ítem, todas estructurales (diálogos nativos, audio real, sin mecanismo para fabricar una interacción pendiente real, acciones que modificarían configuración global real del usuario, o fuera de alcance acordado) — no relleno, ver "Resumen numérico" al final.
>
> **Respaldado por un gate real:** cada `[QA-###]` marcado ✅ tiene una entrada obligatoria en `docs/reference/qa-coverage.json` (evidencia de camino feliz + revisión estática + las 6 categorías dinámicas). `.claude/flujo-hooks/qa-coverage-gate.ps1` bloquea cualquier edición que intente marcar un ítem ✅ sin esa entrada — verificado que bloquea y que deja pasar un ítem con respaldo completo (ver reporte de esta sesión). `.claude/flujo-hooks/qa-static-scan.ps1` genera `docs/reference/qa-static-scan-report.json`, la fuente de `revision_estatica.resumen`.

---

## EPIC-002 — Núcleo de sesión con Claude Code

### FEAT-006/007 — Transporte y captura de la sesión real (`claude-transport.ts`, `claude_transport.rs`)

- ✅ `start_claude_session`/`startSession` — arranca `claude -p --input-format stream-json --output-format stream-json` como hijo real. Probado 4 veces con sesiones reales. `[QA-001]`
- ✅ `send_claude_instruction`/`sendInstruction` — probado con texto normal, vacío, ~24 000 caracteres, emoji/unicode/HTML. `[QA-002]`
- ✅ `interrupt_claude_session`/`interruptSession` — comando real existe, **confirmado que no está conectado a ningún botón de la UI** (hallazgo, no prueba de la función en sí). Reconfirmado 2026-09-04 con grep fresco sobre `App.vue` completo. `[QA-003]`
- ✅ `close_claude_session`/`closeSession` — probado, cierra sesión correctamente. `[QA-004]`
- ✅ Guardia "ya hay una sesión activa" (segundo `start_claude_session` mientras uno corre) — probado, **hallazgo grave**: no hay reconexión real, solo destruir. `[QA-005]`
- ✅ `respond_to_permission_request`/`respondToPermissionRequest` — mecanismo de simulación agregado (`window.__qaInjectPermissionPending`, `App.vue`, mismo camino real que `onPermissionPending`/`receivePendingRequest`). `InteractionRequestCard.vue` verificada en vivo: aparece con la petición inyectada, el contador (`countPendingInteractionRequests`) sube y baja, 2 simultáneas no se confunden, inyección repetida del mismo id no duplica, coexiste con otro overlay abierto (tema) sin interferir, y un `__qaInjectEvent` normal disparado justo después no pisa la petición pendiente. Corregido `lanzamiento-publico` Hito 3. `[QA-006]`
- ✅ `onActivity`/`onNormalizedEvent`/`onPermissionPending`/`onSessionClosed` (listeners de eventos) — probados indirectamente, reciben eventos reales correctamente. `[QA-007]`
- ✅ `onStderr` — grep confirmado 2026-09-04: se exporta pero **ningún archivo lo importa/invoca** fuera de su propia definición — aunque se forzara un stderr real, no hay listener que lo muestre. `[QA-008]`
- ✅ `get_last_session_preference`/`getLastSessionPreference` — confirmado que persiste y se lee (usado por "Retomar"). `[QA-009]`

### FEAT-008 — Normalizador de eventos (`event_normalizer.rs`)

- ✅ `normalize_system` (subtype `init`) — funciona, dispara `SessionStarted` correctamente. `[QA-010]`
- ✅ `normalize_system` (subtype ≠ `init`, ej. `hook_*`, `permission_denied`) — **Hallazgo 1**: cae a `unclassified`, se ve como "no entendí". `[QA-011]`
- ✅ `normalize_result` — **Hallazgo 2**: trata cada fin de turno como fin de sesión completa. `[QA-012]`
- ✅ `normalize_assistant`/`normalize_user` (bloques de texto/tool_use/tool_result) — probado 2026-09-04 con sesión real: texto plano, `tool_use`+`tool_result` de `Read` (numerado tipo `cat -n`), y `tool_use`+`permission_denied`+`tool_result` de `Edit`/`Bash` (bloque `$ comando` con estado "Fallo", sin quedar "En curso" — Hito 1/2 de `correcciones-qa-sesion-real-2` sostenido con casos frescos). Brecha declarada: no se confirmó un bloque de tipo diff real porque el harness deniega todo permiso de escritura/ejecución (ver `[QA-006]`). `[QA-013]`
- ✅ Evento `init` repetido a mitad de sesión — **Hallazgo 9**, reforzado 2026-09-04 con evidencia más limpia: sesión real de 4 turnos, cada uno esperado a que el anterior cerrara su `result` (sin envío concurrente), produjo **4 eventos `init` distintos, uno por turno** (proporción 1:1 turno:init) — confirma que el fenómeno es más amplio que solo el envío simultáneo. Causa raíz exacta sigue sin aislarse, fuera de alcance declarado. `[QA-014]`

### FEAT-009 — Peticiones de interacción y respuesta real (`interaction-requests.ts`, `InteractionRequestCard.vue`, `interaction_request.rs`)

- ✅ `createInteractionRequestsState`/`receivePendingRequest`/`resolveRequest`/`countPendingInteractionRequests`/`listInteractionRequests` probadas en vivo vía `__qaInjectPermissionPending` (tipo `permission`, camino real de aparición/conteo) y vía tipos `confirmation`/`choice`/`open_question`/`form` (única forma de ejercitarlos hoy: `claude-transport.ts:82-89` documenta que ninguno tiene constructor real desde un evento crudo). Aceptar/rechazar un `confirmation` inyectado resuelve por completo en el frontend (`respondConfirmation` no llama a ningún comando Tauri) — ciclo completo verificado: aparece → se acepta/rechaza → pasa a `resolved` → el contador baja → la UI ya no ofrece botones para una petición ya resuelta (`InteractionRequestCard.vue` oculta las acciones tras `decision !== null`). `respondPermission` (rama que sí invoca `respond_to_permission_request`) queda cubierto por las pruebas Rust ya existentes y extensas de `take_pending_request`/`respond_to_permission_request` (`claude_transport.rs`, ver tests `conceder_una_peticion_pendiente_*`, `responder_a_una_peticion_que_no_existe_*`, `responder_dos_veces_*`) — forzar un permiso real end-to-end para esta verificación se descartó deliberadamente (`design.md` D3: "dependencia end-to-end frágil y lenta para QA"). Corregido `lanzamiento-publico` Hito 3. `[QA-015]`

### FEAT-010 — Directorio de trabajo y sesiones

- ✅ Elegir carpeta + iniciar sesión — probado (vía invocación directa, el diálogo nativo de Windows no es automatizable sin `tauri-driver`, límite ya conocido). `[QA-016]`
- ✅ "Retomar `<carpeta>`" — probado, **hallazgo**: no reconecta, repite el mismo error que "Iniciar sesión". `[QA-017]`
- ✅ Cambiar de carpeta con una sesión activa — **confirmado que no existe camino de UI para esto**: `App.vue:668` envuelve todo el panel con "Elegir carpeta" en `v-if="!sessionActive"`; confirmado también visualmente (capturas con sesión activa nunca muestran ese botón). La única salida es cerrar la sesión (Hallazgo 4/`[QA-005]`/`[QA-017]`). `[QA-018]`

### FEAT-031 — Menú visual de comandos (`App.vue`: `openCommandsOverlay`, `selectCommand`, `sendSlashCommand`)

- ⬜ **Nada de este Feature se probó.** El overlay de comandos/slash-commands nunca se abrió ni se envió un slash command real. `[QA-019]`

### FEAT-032 — Panel de plugins, MCP y ajustes (`AdminSettingsPanel.vue`, `claude_admin.rs`)

- ✅ Listar plugins instalados — probado, **Hallazgo 8**: hay duplicados en la lista. `[QA-020]`
- ⬜ Instalar un plugin nuevo (`installAvailablePlugin`/`install_plugin`) — **deliberadamente no probado hoy**: modificaria de forma real y permanente la configuracion global de `~/.claude` del usuario fuera del sandbox de la prueba, fuera del criterio de seguridad de esta pasada. `[QA-021]`
- ⬜ Activar/desactivar un plugin (`togglePlugin`/`set_plugin_enabled`) — **deliberadamente no probado hoy**, mismo motivo que QA-021 (modifica configuracion global real del usuario, aunque sea reversible). `[QA-022]`
- ⬜ Agregar marketplace (`submitAddMarketplace`/`add_plugin_marketplace`) — **deliberadamente no probado hoy**, mismo motivo (accion de red real contra un marketplace real). `[QA-023]`
- ✅ Listar servidores MCP (conectados/fallidos/necesitan auth) — probado, funciona, sin duplicados. `[QA-024]`
- ⬜ Agregar/quitar servidor MCP (`submitAddMcp`/`removeMcp`) — **deliberadamente no probado hoy**, mismo motivo que QA-021/022/023 (modifica configuracion global real del usuario). `[QA-025]`
- ✅ Ver ajustes (solo lectura, global + proyecto) — probado, renderiza JSON real correctamente. `[QA-026]`

---

## EPIC-003 — Contenido y transparencia técnica

### FEAT-011 — Intérprete incremental de contenido (`content-interpreter.ts`)

- ✅ `parseContentBlocks`/`pushStreamChunk`/`flushStreamBuffer` — probado 2026-09-04 en sesión real: texto plano, referencia de archivo (`` `notas.txt` ``/`` `ansi.txt` `` entre backticks), bloque de comando (`$ node -e ...`), todos detectados y renderizados con su tipo correcto. Brecha: bloque de tipo diff real no confirmado (permisos de escritura siempre denegados en este harness, ver `[QA-006]`). Ver también **Hallazgo 18** (`[QA-029]`). `[QA-027]`
- ✅ `armParseFault`/`FAULT_INJECTION_MARKER` — probado en la app real via `DebugPanel.vue` ("Fallo del interprete sobre ese fragmento"): se disparó un fallo real, quedó registrado en `listFailures()` (`[interpretacion/parsing-error] fallo de parseo inyectado para pruebas`), y el resto del turno se renderizó sin corromperse ni tumbar la sesión. `[QA-028]`
- ✅ `stripAnsiSequences` — probado con un archivo real conteniendo bytes ANSI genuinos (confirmados byte a byte con `xxd` antes de la prueba). **Hallazgo 18, causa raíz confirmada (`lanzamiento-publico` Hito 6, 2026-09-13): no es un bug de esta función.** Comparando el log de sesión cruda, el `tool_result` de `Read` sí trae el byte ESC real, pero el texto que el propio modelo genera al citar ese contenido en su respuesta no lo reproduce (limitación de generación del modelo, no de post-procesamiento). `stripAnsiSequences` se reprodujo en vivo dos veces con bytes ESC genuinos (evento único y streaming en fragmentos de 5 caracteres) y limpia correctamente en ambos casos — más prueba de regresión permanente agregada (`content-interpreter.spec.ts`, caso "Hallazgo 18"). No corregible desde este código sin arriesgar falsos positivos sobre contenido legítimo del usuario. `[QA-029]`

### FEAT-012 — Consola de actividad y visor de salida cruda (`activity-console.ts`)

- ✅ `handleNormalizedEvent`/`appendRawActivity` — probado extensamente, **Hallazgo 3/14**: comandos bloqueados quedan "En curso" para siempre; **Hallazgo 16 (2026-09-04, más amplio)**: TODO comando, exitoso o no, queda "En curso" para siempre — `CommandFinished` nunca se construye en el backend. `[QA-030]`
- ✅ Pestaña "Salida cruda" — probada, renderiza JSON real línea por línea. `[QA-031]`
- ✅ Alternar "Estilizada"/"Markdown" en Actividad — probado en la app real, clic real cambia de vista correctamente. `[QA-032]`
- ✅ "Ocultar actividad"/"Ocultar chat" (botones) — probado en la app real, clic real oculta la seccion y el boton cambia a "Mostrar actividad". `[QA-033]`

### FEAT-013 — Chat en flujo continuo (`chat.ts`)

- ✅ `recordPersonMessage`/`applyChatEvent`/`createChatState` — probado extensamente (mensajes normales, vacíos, larguísimos, con caracteres especiales). `[QA-034]`
- ✅ Auto-scroll al mensaje más reciente — **confirmado 2026-09-04** con medición real del DOM: tras 6 turnos reales de conversación, `.chat-panel__entries` midió `scrollTop=0` con `scrollHeight=531` y `clientHeight=400` (lejos del fondo) — el panel no hace auto-scroll al turno más reciente. `[QA-035]`

### FEAT-014 — Interfaz de autorizaciones, opciones y formularios

- ✅ Igual que FEAT-009 — las 4 variantes (permiso/confirmación/opciones/formulario) ahora son inyectables y verificables vía `__qaInjectPermissionPending` (ver `[QA-006]`/`[QA-015]`). Corregido `lanzamiento-publico` Hito 3. `[QA-036]`

---

## EPIC-004 — Presencia VTuber

### FEAT-015 — Abstracción de avatar y personaje de prueba (`avatar-controller.ts`, `avatar-face-presentation.ts`)

- ✅ `TestAvatarController`/`AvatarSnapshot`/`setState`/`setExpression` — probado exhaustivamente vía harness y app real. `[QA-037]`
- ✅ `resolveMood` — probado, y es la pieza que **Hallazgo 6** demuestra que no cubre el vocabulario real de estados. `[QA-038]`
- ⬜ `isMouthOpen`/`resolveMouthPath`/sincronización de boca con audio real — **nunca verificado con audio real** (no se puede confirmar por captura de pantalla). `[QA-039]`
- ✅ `armAvatarDrawFault` (fault injection del avatar) — probado en la app real 2026-09-04: fallo armado desde `DebugPanel.vue`, clic real en "Probar" (fila `sleeping`) dejó `avatarState` en `idle` (el `setState` interno lanzó y fue absorbido); prueba de control inmediata sin el fallo armado SÍ cambió `avatarState` a `sleeping` — confirma que el fallo intercepta la mutación sin tumbar la sesión. Observación menor: `avatarLastError` no queda visible de forma sostenida porque el `setExpression` que sigue en `previewAssignment` lo resetea a `null` al tener éxito. `[QA-040]`

### FEAT-016 — Catálogo de personajes incluidos (`character-catalog.ts`, `character-selection.ts`, `CharacterSelector.vue`)

- ✅ Cambio real de personaje (Alicia Solid → Rabbit) probado en la app real: selector abierto, clic en "Seleccionar", avatar real cambio de modelo y la etiqueta paso a "Personaje: Rabbit" (captura `qa041-rabbit-seleccionado.png`). `persistCharacterId`/persistencia entre reinicios no verificada esta pasada. `[QA-041]`

### FEAT-017 — Importación de personajes propios (`character-import.ts`, `character_import.rs`)

- ⬜ **Parcialmente probado.** `pickCharacterFile()` (`character-file-picker.ts`, extraído en `lanzamiento-publico` Hito 5) queda cubierto por prueba unitaria mockeada: éxito (ruta real devuelta tal cual), cancelación (`null`), selección múltiple inesperada (tratada como `null`, no como ruta valida), y propagación del error del SO sin envolverlo. `openCharacterImportOverlay()` (`App.vue`) sigue abriendo el overlay igual que antes tras el refactor, confirmado en vivo con `__qaOpenCharacterImportOverlayWithPath` (regresión, no ejercita `pickCharacterFile` en sí). El clic dentro del diálogo nativo en sí sigue sin automatizar — **manual permanente**, declarado en `QA/to-prove.md` sección 1.9, mismo patrón que TC-085/098/099 de `robustez-distribucion`. `[QA-042]`

### FEAT-018 — Editor de personajes (`character-editor.ts`, `character-editor-storage.ts`, `CharacterEditor.vue`)

- ✅ Tabla de asignación (expresión/animación/pose por estado) — probada extensamente en la app real, **Hallazgo 5** encontrado y root-causado. `[QA-043]`
- ✅ `resolveActiveStateAssignment`/`assignmentForState`/fallback a fábrica — probado antes/después, limpio. `[QA-044]`
- ✅ `defaultCharacterEditorSettings` — confirmado que puebla los 27 clips correctamente para un personaje sin config previa. `[QA-045]`
- ✅ `applyAnchorChange`/`applySizeChange`/`clampCharacterSize`/`clampAnchorToArea` (tamaño/anclaje/márgenes) — **brecha ya documentada en TC-052, ahora con cierre completo**: se guardan pero nada los renderiza. Reverificado 2026-09-04 en la app real: `Esquina=top-left, Margen X=77, Margen Y=33, Tamaño=1.7` cambiados y guardados (`"Cambios guardados."` visible), confirmado en `localStorage['codetuver-avatar.character-editor.rabbit']` real que SÍ persisten entre acciones — pero grep completo de `anchor`/`marginX`/`marginY`/`size` sobre `App.vue`/`AvatarStage.vue`/`VrmAvatar.vue` confirma **cero consumidores**: se guardan correctamente, nunca se leen para afectar el render. `[QA-046]`
- ✅ `discardEditorChanges`/"Descartar cambios" — probado en la app real: cambio real de expresion (neutral→happy) en un select, clic en "Descartar cambios", el valor volvio a "neutral". `[QA-047]`
- ✅ `previewAssignment`/botón "Probar" por fila — probado en la app real: clic en "Probar" de la fila "success" cambio el avatar real al pool "positive" (`hasAction:true`, `positive-animation-3`), confirmado con `__debugAvatarState()` y captura `qa048-probar-preview.png`. `[QA-048]`
- ⬜ Importar animación/pose (`Elegir archivo (.vrma)`) — diálogo nativo, no automatizado. `[QA-049]`
- ✅ Capacidades deshabilitadas con motivo (`capabilityCellProps`) — comportamiento ya confirmado en TC-054 (personaje importado); no re-probado esta sesión. Se intento reproducir hoy con "Rabbit" (unico personaje incluido sin importar disponible ademas de Alicia Solid) pero Rabbit resulto tener capacidades completas (sin celdas deshabilitadas) — reproducir de nuevo requeriria un personaje importado real, bloqueado por el mismo dialogo nativo que QA-042/049. `[QA-050]`

### FEAT-033 — Autoría de poses y animaciones en la app (`PoseEditor.vue`, `AnimationTimelineEditor.vue`, `vrma-pose-export.ts`, `vrma-animation-export.ts`, `animation-keyframe-list.ts`)

- ✅ Posar un hueso con gizmo + exportar como pose (`exportPoseAsVrma`) — probado end-to-end (vía hook equivalente al gizmo real, no arrastre literal de mouse). `[QA-051]`
- ✅ Capturar keyframes en la línea de tiempo + exportar como animación (`exportAnimationAsVrma`, `upsertKeyframe`, `removeKeyframe`) — probado con arrastre real de mouse (Hito 2) y con la prueba funcional multi-hueso. `[QA-052]`
- ✅ Reproducción de lo creado, incluido loop — probado con datos numéricos concretos. `[QA-053]`
- ✅ Overlay accesible (foco/Escape) de ambos editores — probado con Playwright real. `[QA-054]`
- ✅ `syncTheatreFromLiveBone`/integración con `@theatre/core` como capa observable — probado 2026-09-04 con arrastre real del gizmo (Playwright `mouse.down`/`move`/`up` sobre el anillo del gizmo, no solo apertura): el hueso `head` rotó visiblemente en el modelo 3D real y la etiqueta del selector cambió de `head` a `head (tocado)`, confirmando que el arrastre se propagó al bone real. `[QA-055]`
- ✅ Previsualizar en bucle (botón dedicado del `AnimationTimelineEditor`, `aria-label="Previsualizar en bucle"`) — reconfirmado en la app real, boton real localizado y clic sin error (sin keyframes cargados en esta prueba, asi que no hubo movimiento visible que confirmar mas alla del clic mismo). `[QA-056]`

### FEAT-019 — Motor de personalidad y reacciones (`reaction-catalog.ts`, `reaction-engine.ts`)

- ✅ `resolveReaction`/prioridad/enfriamiento — probado y caracterizado en profundidad, **Hallazgo 2** y **Hallazgo 6**. `[QA-057]`
- ✅ `validateCatalog` — el catálogo de 67 estados se leyó y auditó por completo (base del Hallazgo 6). `[QA-058]`
- ✅ Cobertura de las 83 reacciones del catálogo real por prueba de datos (`reaction-catalog.spec.ts`, describe "cobertura completa del catalogo — Hito 2"): cada entrada con `triggers` reales gana su evento representativo o documenta en `fallback` por qué pierde siempre ante una hermana de mayor prioridad; detección automática de ids duplicados. Reemplaza el ítem antes citado como `[QA-059]` en los resúmenes históricos de 2026-09-04 (mismo hallazgo, retag al corregirse). Corregido `lanzamiento-publico` Hito 2. `[QA-140]`
- ✅ Reacciones con `triggers: []` — confirmado código vivo, no muerto: cada una documenta en su campo `fallback` la razón concreta de no tener trigger propio (ambiental, depende de payload sin exponer, etc.), verificado por prueba de datos que falla si aparece una nueva entrada sin justificar. Corregido `lanzamiento-publico` Hito 2. `[QA-060]`

### FEAT-020 — Voz (`text-to-speech.ts`, `mouth-sync.ts`, `VoiceControls.vue`)

- ⬜ Parcialmente probado: clic real en el checkbox "Activar voz" desactivo correctamente el resto de controles y mostro "Voz desactivada." (captura `qa061-voz-desactivada.png`) — confirma que la reactividad de la UI funciona. **Actualización `lanzamiento-publico` Hito 4:** la cola de frases (`SpeechQueueState`/`acceptPhrase`/`dequeuePhrase`) y el agrupamiento de 2500ms quedan confirmados con audio real via introspeccion de `speechSynthesis` (`window.__qaSpeechLog`, ADR-0005 — sin oido humano): 2 frases dentro de la ventana de 2500ms suprimen la segunda, 2 frases fuera de la ventana suenan ambas en orden, con `startedAt`/`endedAt` reales. `armVoiceFault` (inyector de fallas de `DebugPanel`, mecanismo distinto) sigue sin confirmarse en esta pasada. `[QA-061]`

### FEAT-021 — Sincronización de boca

- ⬜ No probado — depende de audio real, mismo límite que FEAT-020. `[QA-062]`

---

## EPIC-005 — Presentación y escritorio

### FEAT-022 — Gestión de ventana de escritorio (`desktop-window-manager.ts`)

- ✅ Bootstrap fuera de Tauri / resolución perezosa de `getCurrentWindow()` — corregido y verificado esta sesión (parte de `animaciones-editables-correcciones`). `[QA-063]`
- ✅ `getMonitors`/`getCurrentMonitor`/`calculateCornerPosition` (posicionamiento multi-monitor simulado) — probado con `DebugPanel.vue` en la app real: `armDualMonitors` → preset `dual`, `recalculatePetPosition` devolvió `x=1700, y=820, monitor=sim-primary` (cálculo real), `disconnectSecondaryMonitor` volvió a `single` correctamente. `selectMonitor` (con hardware real) sigue sin probar. `[QA-064]`
- ✅ Resto de métodos de `TauriDesktopWindowManager` — confirmado 2026-09-04 que `setFocusable`/`setIgnoreMouseEvents`/`hide`/`show`/`focus` ya se ejercitan en cada transición de modo (`[QA-066]`/`[QA-068]`) y `setSize`/`setPosition` en `pet-window.ts:29-30` al mostrar la ventana PET (`[QA-071]`). Ciclo real PET→FULL ejecutado en la app real (botón "Enviar al escritorio" → "Modo completo") sin errores, confirmando que `hidePetWindow()` se invocó sin fallar. **Brecha deliberada**: no se disparó `windowManager.startDragging()` real (arrastre nativo de ventana) por riesgo de dejar la automatización CDP atascada en un loop de arrastre modal del SO — decisión explícita, no un olvido. `[QA-065]`

### FEAT-023 — Máquina de estados de presentación (`presentation-manager.ts`)

- ✅ Transición `FULL` ↔ `COMPANION` — probada en la app real, sin errores. `[QA-066]`
- ✅ Transición a `PET` — **Hallazgo 7 (corregido, transición real confirmada)**: la transición en sí (no solo la ruta de fallo de `[QA-068]`) ya se confirmó funcionando en `[QA-071]` (segunda ventana real creada, avatar VRM renderizando sobre fondo transparente) y de nuevo en `[QA-065]` (ciclo PET→FULL completo sin errores). `[QA-067]`
- ✅ `armPresentationTransitionFault` probado en la app real via `DebugPanel.vue`: fallo armado con modo destino PET, clic real en "Enviar al escritorio" disparo el fallo, mensaje legible `"fallo de transicion inyectado (dev-only)"` (no `[object Object]`, reconfirma el fix del Hallazgo 7 de forma determinista), y quedo registrado en `listFailures()`. `[QA-068]`
- ⬜ Persistencia de `PersistedPresentationSettings` entre reinicios — probado hoy con un `reload()` real tras cambiar de modo: la app broto con el aviso "No se pudo leer tu configuracion guardada" — **causa raiz encontrada, ver Hallazgo 17**: `setGlobalAppSettings`/`setProjectAppSettings` no tienen ningun llamador real en la UI (QA-076), asi que el archivo de configuracion nunca llega a crearse y la persistencia no es verificable en la practica, no por falta de tiempo sino porque el camino de escritura no existe. `[QA-069]`

### FEAT-024 — Modo completo y modo compañera

- ✅ Ya cubierto arriba (FEAT-023). `[QA-070]`

### FEAT-025 — Modo mascota de escritorio (`PetView.vue`, `pet-window.ts`, `pet-sync.ts`)

- ✅ `showPetWindow` confirmado funcionando en la app real dos veces esta pasada: "Enviar al escritorio" creo una segunda ventana real (segundo `page id` en CDP) con el avatar VRM real renderizando correctamente sobre fondo transparente (captura `qa067-pet-transicion.png`). `hidePetWindow`/arrastre (`onPetMouseDown/Move/Up`)/`applySync` siguen sin probar — evidencia parcial, no exhaustiva. `[QA-071]`

### FEAT-026 — Señales de atención e interacciones pendientes (`attention-signals.ts`, `attention-signal-settings.ts`)

- ⬜ **Nada de este Feature se probó.** `resolveAttentionSignal`, `ATTENTION_SIGNAL_MESSAGE`, `persistAttentionSignalSettings`/`loadAttentionSignalSettings`, ni `notifyOnRisingEdgeInPetMode` (que además depende del modo mascota, bloqueado por el Hallazgo 7). `[QA-072]`

---

## EPIC-006 — Robustez, configuración y distribución

### FEAT-027 — Clasificación de errores y aislamiento de fallas (`failure-taxonomy.ts`)

- ✅ `registerFailure`/`listFailures` probado en la app real: 3 fallas reales inyectadas via `DebugPanel.vue` (interpretacion/parsing-error x2, ventana/mode-change-error x1) aparecieron correctamente listadas con su categoria y mensaje en el panel de Diagnostico. `clearFailureLog` no se probo. `[QA-073]`
- ✅ `DebugPanel.vue` — probado en la app real: atajo `Ctrl+Shift+Alt+D` lo abre, `Escape` lo cierra, botones de simulación de monitores/fallo de transición/capacidad ausente/inyección de evento responden sin error. **Hallazgo 11**: el foco no se mueve al botón "Cerrar" al abrirse (viola su propio contrato de diálogo modal accesible). `[QA-074]`

### FEAT-028 — Configuración persistente (`app-settings.ts`, `app_settings.rs`)

- ✅ Lectura de ajustes global/proyecto (solo lectura en la UI) — probada, funciona. `[QA-075]`
- ✅ `setGlobalAppSettings`/`setProjectAppSettings` (escritura) — la UI actual es de solo lectura, no hay control para probar escritura desde la app. Confirmado con `grep -rn "setGlobalAppSettings\|setProjectAppSettings" src/` (excluyendo specs), reconfirmado 2026-09-04: **cero llamadores reales en toda la app** — esta ausencia de UI es, con certeza, la causa raiz de Hallazgo 17/QA-069. `[QA-076]`

### FEAT-029 — Pruebas de compatibilidad y rendimiento de escritorio

- ⬜ No abordado — fuera del alcance de lo que se puede verificar por captura de pantalla (rendimiento real, distintas resoluciones/DPI). `[QA-077]`

### FEAT-030 — Documentación de instalación, licencias y distribución

- N/A para QA funcional — es documentación, no código ejecutable.

---

## Accesibilidad y navegación transversal (`tab-navigation.ts`)

- ✅ `nextTabId`/`handleTabListKeydown` probado con teclado real en la app real: `ArrowRight`/`ArrowLeft` mueven el foco y `aria-selected` correctamente entre pestañas (captura `qa078-tab-keyboard-nav.png`). `[QA-078]`

## revision-ux-sesion-real-3 — H1: paleta neutra + primario naranja (`constants.css`)

- ✅ Los 11 tokens con hue 275/195 pasan a `oklch(L 0 0)`; `--color-primary-rgb`/`--color-accent-primary`/`--color-chat-bubble` nuevos (D1); `--shadow-glow-accent` desquemado. Contraste medido con pixeles reales compuestos en canvas (no estimado), todos los pares ≥4.5:1 (AA). Verificado en vivo contra la app real (CDP 9222) en Modo Completo y Compañera, con blur activo y desactivado: cero morado/azul de token (capturas `qa079-*.png`, enviadas al chat). **Hallazgo real corregido**: `.chat-panel__entry--person` no consumía `--color-chat-bubble` (D1 lo definía sin cablear); y los controles nativos (sliders/checkboxes) seguían en azul de sistema por `accent-color` sin fijar — ambos corregidos en el mismo hito. `[QA-079]`

## revision-ux-sesion-real-3 — H2: popup de tema con editor de color (`ThemePopup.vue`, `theme-color.ts`)

- ✅ `iro.ColorPicker` montado sin slider de alfa (candado estructural, D2); persistencia real vía `AppSettings.primaryColorRgb` (TS + Rust, mismo mecanismo que `chatColumnWidthPx`, no `localStorage`). Verificado en vivo por CDP: click en la rueda aplica el color al instante (`color:change`), un reload completo de la app real confirma que sobrevive (`--color-primary-rgb` idéntico post-reload, y visualmente en el tab "Modo completo" tras retomar la sesión), y el botón de reset también persiste. Menú nativo (`Window > Theme`) sin equivalente DOM — se agregó `window.__qaOpenTheme`, mismo patrón que `__qaSelectCharacter`. 22 pruebas unitarias nuevas, 1178/1178 de la suite completa sin regresión. `[QA-080]`

## revision-ux-sesion-real-3 — H3: barra de scroll global (`scrollbar.css`)

- ✅ Hoja global nueva (`*::-webkit-scrollbar*`), sin wrapper por zona — cubre las 16 zonas con `overflow-y/overflow: auto` existentes en 6 archivos por herencia de selector universal, cero estilos locales pisándola. Verificado en vivo contra la app real: `getComputedStyle(el, '::-webkit-scrollbar-thumb')` mide `border-width` 3px en reposo → 0px tras un hover real (`matches(':hover')`), confirmado en un textarea real (`.chat-panel__input`) sin ningún wrapper agregado. `[QA-081]`

## revision-ux-sesion-real-3 — H4: iconos (`icons.ts`, `IconGlyph.vue`)

- ✅ 13 paths nuevos + componente `IconGlyph.vue` que absorbe las 3 copias de `<svg><path>` repetidas. Los 13 botones de acción del editor de personaje pasan de texto a icono conservando su `aria-label`, verificado en vivo por DOM (`textContent` vacío + `aria-label` intacto en los 13, fila "idle" real). Los 15 usos de `.session-panel__error`/`.session-panel__notice` en 5 archivos ahora llevan icono delante del texto. **Hallazgo lateral no atribuible a este hito**: la pestaña activa del tablist secundario es invisible contra un escritorio Mica claro (`color: var(--color-text-primary)` sin fondo) — ya presente en capturas de H1, no corregido aquí por estar fuera de alcance. `[QA-082]`

## revision-ux-sesion-real-3 — H3 (corrección): scrollbar se quedaba pegado engrosado

Hallazgo real del usuario probando la app corriendo (no detectado por la verificación automatizada original de H3): el hover dependía del contenedor completo, no del pulgar mismo, y no revertía al quitar el mouse. Corregido a `::-webkit-scrollbar-thumb:hover` + `transition`. La verificación de que el hover-out ahora revierte de forma fiable contra el compositor nativo de WebView2 no se pudo confirmar por automatización con la misma certeza que el resto del proyecto — pendiente de confirmación directa del usuario. `[QA-081]` actualizado.

## revision-ux-sesion-real-3 — H5: el avatar llena su contenedor (`VrmAvatar.vue`, `vrm-camera-framing.ts`)

- ✅ Canvas a 100%/100% de su contenedor; `ResizeObserver` propio conectado/desconectado en el ciclo de vida; encuadre de cámara ("contain", nunca deforma) calculado con una función pura nueva (7 pruebas). Se retiró por completo el prop `large` y los 2 tokens de tamaño fijo que ya no tenían uso (confirmado por grep). Verificado en vivo contra la app real en las 3 relaciones de aspecto (ancha real, cuadrada y alta forzadas por override temporal ya que el layout real no produce un contenedor angosto) y en los 3 puntos de montaje (Avatar, Modo Compañera — notablemente más grande ahora —, Editor de personaje): el modelo nunca se deforma, siempre dentro de sus bordes con aire en el eje que no restringe. `[QA-083]`

## revision-ux-sesion-real-3 — H6: interpolación real entre poses y animaciones (`VrmAvatar.vue`)

- ✅ `resetBodyAnimationState()` ya no mata la acción saliente antes de que exista la entrante (causa raíz del corte seco); `ensurePoseAction()` cambia su guardia a comparación por URL, permitiendo cambiar de pose dentro del mismo estado. Duración mínima subida a 750ms. Verificado en vivo con `__debugAvatarState()`: un cambio de estado real muestra el peso de la animación entrante subiendo medible (0.10→0.73 en ~600ms), y con duración 0 guardada el peso es 1 desde el primer frame (corte inmediato real, AC-082). **3 sub-verificaciones no probadas en vivo esta pasada, declaradas**: crossfade pose-a-pose, acumulación de rotación entre 2 poses aditivas simultáneas (riesgo marcado por el plan, no confirmado como bug), e interpolación en modo mascota (confirmado por lectura de código, no en una ventana PET real). `[QA-084]`

## revision-ux-sesion-real-3 — H7: ejecutar y editar una animación concreta (`vrma-animation-import.ts`, `vrm-clip-resolver.ts`)

- ✅ "Ejecutar" en cada chip de animación reproduce esa animación puntual en el avatar en vivo (canal `previewClipId`/`previewNonce`), no la fila entera; "Editar" precarga los keyframes reales del clip (parser inverso nuevo). **2 hallazgos reales encontrados y corregidos en vivo contra la app real**: (1) el ciclo de fondo pisaba la vista previa al frame siguiente porque no sabía que había una vista previa activa — corregido sincronizando el estado del pool; (2) el parser inverso buscaba el hueso normalizado ("head") pero los clips reales llegan con el nombre del nodo raw del VRM ("Head") — corregido con un resolver nuevo (`boneNameFromRawNodeName`). Verificado en vivo: AC-083 (cada fila de un pool de 3 reproduce la suya, confirmado por `playingAnimationUrl` exacto), AC-084 (doble click reinicia `actionTime` a 0 exacto), AC-085 (3 keyframes reales precargados, sin error). Si el clip no se puede leer, el editor completo se reemplaza por un panel de error — nunca un editor vacío que aparente ser la animación (AC-087, verificado por pruebas unitarias; sin reproducción en vivo posible porque el catálogo actual no tiene ningún clip que dispare ese camino). `[QA-085]`

## revision-ux-sesion-real-3 — H8: modo mascota sin borde ni scroll, con tamaño ajustable (`pet-window.ts`, `pet-window-size.ts`)

- ✅ Slider "Tamaño de la mascota" en Modo Compañera (100-400px, persistido en `AppSettings.windowSize`, mecanismo ya instalado — sin multiplicador nuevo). El borde reportado tenía dos causas, no una: `shadow: false` (ya presente) más `window.remove_menu()` nuevo porque `menu.setAsAppMenu()` adjuntaba el menú nativo a la mascota por omisión. **3 hallazgos reales encontrados y corregidos en vivo contra la app real, no anticipados por el plan**: (1) la primera apertura de la mascota en cada proceso se quedaba en 800x600 (el tamaño por omisión de Tauri) — `setSize` corría contra un webview aún no creado por Tauri; (2) un recargo del frontend no destruye el webview real, así que reabrir la mascota lanzaba "a webview with label `pet` already exists" en vez de reusarla; (3) incluso sin sombra ni menú, la mascota seguía reservando ~20px de alto. Verificado en vivo (CDP): ancho exacto en 100/150/250/300/350px, persistencia real confirmada por `get_app_settings`, clic para restaurar y arrastre siguen funcionando con el tamaño cambiado, captura en 100x100 (proceso recién iniciado) sin borde ni barra visible. **Brecha declarada**: el alto queda consistentemente +20px por encima del solicitado incluso tras quitar el menú — no genera borde ni scroll visibles, causa exacta no diagnosticable con las herramientas disponibles (requeriría inspección Win32 nativa), documentada en D10 de `design.md`. `[QA-086]`

## revision-ux-sesion-real-3 — H9: el chat aguanta anchos chicos sin scroll horizontal (`App.vue`, D11)

- ✅ `overflow-x: hidden` en `.chat-panel__entries` y `overflow-wrap: anywhere` en `.chat-panel__entry` (heredado a todo el texto del mensaje en un solo punto). **La hipótesis original de D11 (`min-width: 0` en la cadena `.full-layout → li`) se refutó con medición real**, no se aplicó a ciegas: con el separador al mínimo, la cadena de contenedores se midió en 216-240px sin ensancharse (el track del grid es px explícito, sin el quirk de "mínimo automático"; los contenedores flex intermedios son de columna, donde ese quirk no afecta el ancho). La causa real medida fue `.content-blocks__file-path` (una ruta larga sin espacios) renderizándose a 894px sin ningún wrap, forzando `overflow-x` de la lista a `auto`. Verificado en vivo con un mensaje real inyectado (tabla + ruta larga) vía `window.__qaInjectEvent` nuevo (generaliza el `onIncomingEvent` que `DebugPanel` ya recibía, sin gastar sesión real de Claude): sin barra horizontal en 3 anchos distintos (240px, ~280-400px, ~1128px). **Brecha declarada**: una tabla de 3 columnas con valores largos se lee mal a 240px (partición letra por letra) aunque se mantiene contenida sin escapar; a ~400px la misma tabla ya envuelve de forma legible — no se subió `MIN_CHAT_COLUMN_WIDTH_PX` porque ningún mínimo razonable arregla una tabla de 3 columnas con valores largos. `[QA-087]`

## revision-ux-sesion-real-3 — H10: tabs reales de Modo completo y Modo compañera (`App.vue`, D12)

- ✅ El tablist de modos pasa de `.session-panel__button` a `.secondary-tabs__bar`/`.secondary-tabs__tab` (mismo lenguaje visual que Avatar/Actividad/Salida cruda), sin tocar `role`/`aria-selected`/`tabindex` itinerante. Se agregó `.secondary-tabs__tab:disabled` (no existía) para no perder la señal visual que `.session-panel__button:disabled` sí daba durante una transición — verificado en vivo forzando `disabled=true`. **Hallazgo real fuera de alcance, declarado, no corregido**: capturado vía CDP, el texto del tab activo se ve casi invisible contra el fondo que Chromium compone para las superficies translúcidas de la app (`--color-surface-base`/`raised`, diseñadas para un escritorio oscuro detrás de la ventana transparente de Tauri) — pero el tab "Avatar" preexistente (misma clase, sin tocar) se ve idéntico, así que no lo causó este hito; no está confirmado si es un bug real o un artefacto de que CDP no reproduce la transparencia real de una ventana Tauri contra el escritorio — reportado al usuario para confirmar en la ventana real. `[QA-088]`

## revision-ux-sesion-real-3 — H11: composer de modo completo con Markdown real (`App.vue`, `chat-draft-markdown.ts`, D8)

- ✅ `markdown-it` reemplaza `ContentBlockRenderer`/`parseContentBlocks` en la vista previa del borrador (ese parser interpreta la salida del agente, no es un renderizador Markdown — por eso un encabezado y un párrafo en líneas separadas salían como una sola línea sin formato). El composer expandido ahora ocupa el alto completo del panel de chat (se retiran los topes fijos `--size-chat-composer-expanded-min/max`, se oculta la lista de mensajes mientras está expandido) y presenta 2 pestañas reales, Editar/Vista previa, reusando el mismo patrón de tabs que el resto de la app; el `<textarea>` pasa de `v-else` a `v-show` para no perder cursor ni contenido al alternar. **AC-092 resultó ser el propio estado "expandido" del composer, no el modo de presentación FULL de la app** — mismo nombre, dos conceptos distintos; se verificó contra spec.md antes de implementar para no aplicar el paso equivocado. Verificado en vivo (CDP) con el borrador exacto del reporte: el composer expandido mide el alto completo del panel, la vista previa renderiza `<h1>`/`<p>` como elementos separados, y el `<textarea>` es el mismo nodo DOM (con su valor intacto) antes y después de alternar pestañas. `[QA-089]`

## revision-ux-sesion-real-3 — H12: listado de comandos anclado sobre la caja de texto (`App.vue`, `slash-commands.ts`, D9)

- ✅ El diálogo modal de comandos (`.commands-overlay`, con input de búsqueda propio y un paso separado de "argumentos") se retira; un `<ul role="listbox">` anclado con `position:absolute; bottom:100%` aparece directamente sobre la caja de texto, dentro del propio formulario del composer. El foco nunca sale del `<textarea>`: gana `role="combobox"`/`aria-expanded`/`aria-controls`/`aria-activedescendant`/`aria-autocomplete` (el patrón ARIA de textbox+listbox), el filtro sale de lo que ya se escribió después del "/", flechas mueven la selección, Enter escribe el comando en el borrador sin enviarlo, Escape cierra sin tocar nada. La entrada del menú nativo ("Comandos") ahora abre el mismo listado (escribe "/" y enfoca el textarea) en vez de quedar apuntando a un diálogo retirado. Verificado en vivo (CDP) de punta a punta con comandos simulados: filtrado, navegación con flechas sin robar el foco, commit por Enter sin enviar nada, cierre por Escape sin tocar el borrador, una ruta con "/" en medio que nunca abre el listado, y el disparador del menú nativo llegando al mismo listado. `[QA-090]`

## revision-ux-sesion-real-3 — H13: la voz omite código y resume tablas (`content-block-text.ts`, D7)

- ✅ `blocksToSpeechText(blocks)` nueva, hermana pura de `blocksToMarkdownText`: prosa (párrafo/plano/encabezado/aviso/error) se habla tal cual; código, comandos, diffs y arte ASCII se omiten enteros sin dejar hueco (la prosa alrededor queda continua); una tabla se reemplaza por "Hay una tabla." en vez de leerse celda por celda. Única llamada cambiada: el texto que arma `assistant_turn_complete` para la voz — la llamada que alimenta la vista de texto de la consola de Actividad (que sí debe mostrarlo todo) se dejó intacta a propósito. `reaction-engine.ts` (frases del catálogo) no se tocó. Cubierto con 9 pruebas unitarias, incluido el caso literal del reporte (`ls -la` + tabla de juegos: solo se habla la prosa alrededor). `[QA-091]`

## revision-ux-sesion-real-3 — H14: investigación de voces y su ADR (hito de investigación, `docs/adr/0013-voces-anime-y-pitch.md`)

- ✅ Sin código de producción — hito de investigación. Volcado real (CDP contra la app corriendo) de `speechSynthesis.getVoices()`: solo 2 voces instaladas, ambas SAPI clásicas en español (Raul por omisión, Sabina), ninguna neuronal, ninguna de estilo anime. Confirmado por lectura de código que `buildUtterance()` nunca fija `pitch`. VOICEVOX evaluado (API HTTP local real en `localhost:50021`, técnicamente viable) pero descartado por ahora por la barrera de idioma (proyecto/instalador/voces en japonés vs. app en español). Hallazgos presentados al usuario, que decidió **probar `pitch` con las voces ya instaladas antes de considerar cambiar de motor** — decisión registrada en ADR-0013; la implementación de `pitch` queda para un hito nuevo, no se hizo aquí. `[QA-092]`

## revision-ux-sesion-real-3 — H15: menú nativo — orden, copiar id y cerrar sesión (`native-menu.ts`, `App.vue`, D14)

- ✅ El menú queda en el orden Window, Sesión, Character, Comandos. "Comandos" pasa de `MenuItem` suelto (confirmado en el código: literalmente en medio de 3 `Submenu`, la causa estructural del defecto visual reportado) a `Submenu` consistente con las demás entradas. El id de sesión se habilita solo con sesión real y su acción copia al portapapeles confirmando en su propio texto ("(copiado)"). "Cerrar sesión" nueva, cableada al comando de backend que ya existía — verificado en vivo que tras cerrar, la interfaz vuelve genuinamente a la pantalla de "Elegir carpeta". **Brecha declarada**: la confirmación visual pixel a pixel de que el menú nativo se ve consistente no es posible vía CDP (un menú nativo no tiene equivalente en el DOM) — queda pendiente de que el usuario la vea en la ventana real, limitación estructural ya conocida del proyecto. `[QA-093]`

## revision-ux-sesion-real-3 — H16: lector de historial de sesiones (`session_history.rs`, D15)

- ✅ `list_project_folders`/`list_project_sessions` nuevos: leen `~/.claude/projects/` (respetando `CLAUDE_CONFIG_DIR`) sin cargar ningún archivo entero — avanzan desde el principio hasta la primera línea de usuario útil, y leen solo la cola (256KB) para el último `cost-state`. Todo campo es opcional y cualquier fallo de parseo degrada a `None` sin propagarse, nunca rompe el listado. Verificado en vivo contra los datos reales de la máquina: 46 carpetas reales, 28 sesiones reales de este proyecto (incluida una de 3MB) listadas en 78ms, y **5 de esas 28 sesiones reales no tienen `cost-state`** (interrumpidas o de prueba) — se listan igual, con esos campos en `null`, ejercitando la degradación de AC-106 con datos reales, no solo fixtures. `[QA-094]`

## revision-ux-sesion-real-3 — H17: explorador de historial (`SessionHistoryExplorer.vue`, `session-history-format.ts`)

- ✅ La pantalla previa a iniciar sesión gana una cuadrícula de carpetas (icono + nombre); abrir una cambia a un listado (reusa `ScrollableListPanel`) de barras de sesión — icono de Claude + título truncado, y debajo tokens/costo/duración/fecha — con botón de retroceso. Verificado en vivo contra datos reales: las 46 carpetas reales, las 28 sesiones reales de este proyecto con sus datos correctos, una sesión con costo exactamente `$0.00` (no confundida con "desconocido"), y una de las 5 sesiones reales sin `cost-state` mostrando el estado degradado completo en vez de una fila en blanco. `[QA-095]`

## revision-ux-sesion-real-3 — H19: verificación de paridad contra el binario instalado (`docs/reference/paridad-claude-code.md`, D16)

- ✅ Hito de investigación pura, sin código de producción. **La pregunta que gobernaba todo el carril caliente queda resuelta con evidencia real**: un comando slash enviado por el stdin de una sesión `-p --input-format stream-json` real sí se ejecuta como comando (`/context` real produjo `total_cost_usd:0`/`"model":"<synthetic>"` — cero llamadas al modelo — con un reporte estructurado exacto que ningún LLM inventa). Probado uno por uno contra una sesión real: `/context`, `/model` (consulta), `/effort medium` (cambia el estado real), `/usage` y `/compact` **funcionan**; `/permissions` y `/add-dir` **se rechazan explícitamente** ("isn't available in this environment") — un hallazgo real no anticipado, ambos estaban listados como candidatos. También se confirmó que `--max-turns` (citado como flag de arranque en el inventario previo) **no existe** en `claude --help` de la versión real instalada (2.1.263) — se descarta de H20. `[QA-096]`

## revision-ux-sesion-real-3 — H20: opciones de arranque de sesión (`SessionStartOptionsPanel.vue`, `claude_transport.rs`, D16)

- ✅ Panel nuevo en la pantalla previa a iniciar sesión: modelo, modo de permisos (los 6 valores reales confirmados por H19, sin texto libre), directorios adicionales, herramientas permitidas/denegadas y límite de gasto — cada campo cita el flag real que lo respalda. `--max-turns` excluido a propósito (H19 confirmó que no existe en el binario real). Validación en dos capas (TS bloquea el botón, Rust valida de nuevo sin confiar en el frontend) antes de que cualquier valor se convierta en argumento de un proceso real. Se persiste tras un arranque exitoso. **Verificado en vivo con el hallazgo más fuerte del hito**: con `--permission-mode plan` real, pedir explícitamente crear un archivo fue rechazado por el proceso real — escribió un plan, intentó `ExitPlanMode` (que falló) y respondió que no podía crear el archivo en modo plan. No es una opción que solo se pasa: cambia el comportamiento real del binario real. `[QA-097]`

## revision-ux-sesion-real-3 — H21: opciones en caliente (`session-hot-options.ts`, D16)

- ✅ De toda la lista que H19 confirmó, **solo `/effort` calificaba como una opción real con cambio de estado demostrado** (`/context`/`/usage` son reportes de solo lectura; `/compact` es una acción puntual sin valor persistente) — el hito se implementó deliberadamente pequeño en vez de inventar controles para lo que no se confirmó. Panel "Opciones en caliente" en la pestaña Configuración de una sesión activa, que reusa el mismo mecanismo de envío ya probado (`sendChatMessage`). **Hallazgo real de streaming encontrado y corregido durante la propia verificación en vivo**: la respuesta llega incrementalmente, así que la primera versión del código fijaba "sin confirmar" al leer el primer fragmento parcial, antes de que llegara el texto real de confirmación — corregido para reevaluar hasta que el turno realmente termina. Verificado en vivo dos veces (mostrando el bug, y ya corregido) contra un proceso real: enviar `/effort low` terminó en "Confirmado por la sesión real." `/permissions` y `/add-dir` (rechazadas por H19) no se exponen como calientes — ya tienen su equivalente real en el carril de arranque de H20. `[QA-098]`

## revision-ux-sesion-real-3 — H22: reanudar y bifurcar una sesión (`session_history.rs`, `session-transcript.ts`, `SessionHistoryExplorer.vue`, AC-113)

- ✅ Botones "Reanudar"/"Bifurcar" por sesión listada (solo cuando `session.cwd` es real), cableados a `--resume <id>` / `--resume <id> --fork-session`. **Dos hallazgos reales encontrados y corregidos durante la propia verificación en vivo, no en revisión de código:** (1) el proceso reanudado nunca reemite los turnos previos como eventos de stream-json — la interfaz se veía en blanco hasta el primer mensaje nuevo pese a que el modelo sí recordaba el contexto (confirmado preguntándole y respondiendo correctamente) — corregido leyendo el `.jsonl` real (`read_session_transcript`) y sembrando el chat antes de arrancar; (2) un id inexistente no lanza una excepción al invocar (el proceso siempre logra arrancar) sino que falla después con un evento `error_during_execution` sin `session_started` — la app se quedaba en un estado "activo" silencioso sin avisar nada, corregido detectando `session_finished{is_error:true}` con `sessionId` aún `null` y mostrando el error en la pantalla previa. Verificado en vivo end-to-end contra sesiones reales de esta máquina: bifurcar produjo un id nuevo (`722c353e-...` → `c4e8cdf9-...`) con el archivo original con el mismo hash MD5 antes y después; reanudar sin bifurcar mostró la conversación restaurada de inmediato, sin turno nuevo; un id inventado mostró "No se pudo reanudar la sesión..." y dejó la app usable. `[QA-099]`

## revision-ux-sesion-real-3 — H18: ultimas 5 sesiones en el menu Session (`native-menu.ts`, AC-102)

- ✅ El submenu Sesion gana una seccion dinamica con las 5 sesiones mas recientes de la carpeta activa (ordenadas por fecha descendente, truncadas a 5), insertada/removida en cada refresco via `setRecentSessions` en vez de reconstruir el menu entero. Elegir una reanuda esa sesion por el mismo mecanismo real de H22 (`resumeSession`, sin bifurcar). Se refresca cuando cambia la carpeta activa (mismo `watch` que ya alimentaba `setSessionInfo`). Un fallo real de lectura muestra "No se pudo leer el historial" y una carpeta sin historial muestra "Sin sesiones recientes" -- dos avisos distintos, nunca un menu vacio sin explicacion (AC-106). **Brecha declarada, misma limitacion estructural que H15**: un menu nativo de Tauri no tiene equivalente en el DOM, asi que la captura visual pixel a pixel del submenu poblado no es posible via CDP -- en su lugar se verifico en vivo que la carpeta activa real de esta maquina dispara la consulta real (`list_project_sessions` con la carpeta calculada por `cwdToProjectFolder`) y devuelve exactamente las 5 sesiones reales mas recientes en el orden correcto, con la logica de insercion/reemplazo de items cubierta por 7 pruebas unitarias nuevas. `[QA-100]`

## revision-ux-sesion-real-3 — H23: hito de QA (cierre transversal, AC-069 a AC-114)

- ✅ **Catalogo combinatorio via `flow-test-gen`**: flujo real "sesion de Claude Code" modelado con 7 estados lineales + 10 aristas de rollback/compensacion (16 transiciones totales), catalogo de 8 casos aprobados por el usuario, `flow-coverage-check.ps1` confirmo cobertura completa (16/16). Ejecutados en vivo contra el proceso real (no como suite Playwright/Cucumber nueva -- decision deliberada: el proyecto deja las etapas 7/9 del guantelete deshabilitadas a proposito, y toda la verificacion de este plan ya fue en vivo via CDP, no automatizada; construir infraestructura E2E nueva para un solo hito de cierre hubiera sido sobre-ingenieria). Los 8 casos, con evidencia real: dos turnos seguidos en la misma sesion, cierre directo sin turno, colision con sesion huerfana + 2 caminos de recuperacion reales (reconectar sin cerrar, cerrar y reintentar con exito), reanudar, bifurcar, id inexistente -- cubren los mismos flujos que H15-H22 ya habian verificado en vivo, mas dos casos frescos (round-trip de dos turnos, recuperacion completa tras error).
- ✅ **Hallazgo real nuevo, en dos rondas, encontrado por el usuario probando la app en vivo mientras se revisaba este hito, no por una prueba automatizada**: el avatar se veia encuadrado desde la cadera (no desde el pecho) y con la cabeza inclinada hacia abajo ocultando parte de la cara. **Ronda 1**: `CHEST_UP_MIN_HEIGHT_RATIO = 0.45` (`VrmAvatar.vue`) es MAYOR que la proporcion real pecho-a-cabeza de un modelo humanoide normal (0.37 medido en vivo), asi que la salvaguarda para casos patologicos (cuernos/antenas) sustituia SIEMPRE el calculo correcto del hueso `chest`. Corregido a 0.2 -- mejora real pero insuficiente: el usuario, mirando la ventana real (no la captura de CDP), señalo con una captura marcada a mano (rectangulo verde = encuadre correcto, lineas rojas = ensanchamiento en V) que la inclinacion seguia. **Ronda 2 (causa raiz real)**: `CAMERA_VIEW_DIRECTION` copiaba literalmente `camera.position.set(0, 1.35, 1.6)` de `vrm-scene.ts` -- una posicion pensada para una camara a altura de pecho mirando hacia ABAJO a un personaje de CUERPO COMPLETO parado en el suelo (~40° de picado). Usada como direccion de encuadre contra un objetivo que YA esta a altura de pecho, producia una camara mirando hacia abajo desde por encima de la cabeza -- la V roja que el usuario marco es la firma clasica de una camara cercana en angulo pronunciado. Corregido a `(0, 0, 1)`, mirada perfectamente horizontal. **Verificado en vivo por el propio usuario mirando la ventana real** ("Bien, ya lo acomodaste en angulo!"), ambas correcciones complementarias (una fija cuanto cuerpo se ve, la otra desde que angulo).
- ✅ **Re-verificacion de los 5 criterios que el plan anterior dio por cerrados sin cumplirse**: AC-049 (composer Markdown) confirmado en vivo con renderizado real (negrita/cursiva/listas/codigo) y panel a la altura completa del contenedor; AC-037/038 (interpolacion) confirmado por inspeccion de codigo sin regresion (`resetBodyAnimationState` ya no mata la accion saliente antes de fundir) mas veredicto independiente de `fresh-verifier`; AC-043 (avatar llena su contenedor) confirmado (`ResizeObserver` real conectado) mas el hallazgo nuevo de arriba encontrado y corregido; AC-052 (tabs de modo) confirmado por inspeccion de DOM/ARIA real (`role="tab"`, texto y clases correctas -- la invisibilidad en la captura es el artefacto de CDP ya declarado en H10, no un bug nuevo); AC-065/066 (sin azul/morado) confirmado por auditoria completa de `constants.css` -- cero tokens con croma fuera de los semanticos de estado, primario por omision `rgb(217,119,87)` (naranja).
- ✅ **Barrido de los 10 fallos y 9 mejoras del reporte original**: cada uno mapea a un hito ya cerrado con su propia evidencia en vivo (Clase B: H7-H9, H13; Mejoras: H2-H4, H8, H12, H14-H22); re-verificacion adicional en este hito sobre "Ejecutar" de animaciones concretas (H7): confirmado que `emit('preview-clip', id)` pasa el id real, no el estado compartido del bug original.
- ✅ **`fresh-verifier`**: veredicto **CONFIRMADO** sobre una muestra de 8 hitos de mayor riesgo (H1, H5, H6, H7, H11, H16, H20, H22) contrastando cada nota de cierre contra el codigo real linea por linea -- sin afirmaciones sin respaldo, sin discrepancias ocultas; senalo correctamente el cambio de `CHEST_UP_MIN_HEIGHT_RATIO` (0.45→0.2) como un hallazgo explicado por comentario, no una discrepancia sin explicar.
- **Brecha declarada**: no se generaron pruebas E2E/Gherkin nuevas para el catalogo de `flow-test-gen` (decision razonada arriba); la re-verificacion de los 10+9 items del reporte original se apoyo en la evidencia ya real de cada hito cerrado mas una muestra fresca dirigida a los puntos de mayor riesgo, no en repetir cada uno desde cero. `[QA-101]`

## revision-ux-sesion-real-4 — H1: causa raiz real del listado de comandos (`event_normalizer.rs`, `slash-commands.ts`, `App.vue`, D1)

- ✅ **Causa raiz real encontrada en vivo, distinta de la hipotesis inicial**: no era un bug de filtrado ni de la capa Vue. `beginSession` marca `sessionActive=true` de inmediato al arrancar el proceso `claude -p --input-format stream-json`, pero Claude Code **no emite `system/init` (de donde salen `session_id` y `slash_commands`) hasta que recibe la primera linea de `stdin`** -- confirmado dos veces: contra la app real via CDP (`window.__qaBeginSession`), `sessionId` se quedo en `null` mas de 60s sin mandar mensaje, con el registro de actividad congelado en el hook de arranque; y aislando la variable, un `claude -p` lanzado a mano sin escribir nada a `stdin` se queda esperando indefinidamente tras el mismo hook, sin emitir `init` nunca. La ventana "sesion activa pero sin comandos" es real y esperada por diseno del protocolo, no un bug a corregir en el backend.
- ✅ **Formato real de `terminal_slash_commands` confirmado y corregido en la documentacion**: contra una captura real de `system.init` (Claude Code v2.1.263) es exactamente `["doctor","color","reload-plugins"]` -- **no** incluye `clear`/`compact` como se asumia originalmente; `slash_commands` real trae 107-110 comandos, incluyendo `clear`/`compact`/`effort`/`usage`/`model`/`mcp` como enviables. Test nuevo en `event_normalizer.rs` (`formato_real_2_1_263_incluye_clear_y_compact_como_enviables`) fija este contrato real sin tocar la logica de `sendable_commands()`, que ya era correcta.
- ✅ **3 estados vacios distinguidos**, donde antes habia un unico "Sin coincidencias" para 3 causas distintas: funcion pura nueva `resolveCommandsEmptyStateMessage()` (`slash-commands.ts`), consumida por un computed en `App.vue`. Verificado en vivo contra la app real los 3 casos: "Los comandos se habilitan despues de tu primer mensaje." (sesion sin inicializar), "Este proyecto no tiene comandos personalizados." (sesion real inicializada via `__qaInjectEvent` con `sendable_commands` vacio), "Sin coincidencias para «zzzznomatch»." (107 comandos reales, texto sin match) -- y recuperacion confirmada: escribir despues `/clear` muestra el comando real de inmediato, sin quedar atascado. `solid-guardian` PASS (funcion pura reutiliza `filterCommands` ya existente, cero acoplamiento nuevo, fronteras de `ARCHITECTURE.md` intactas). `cargo test` 22/22, `vitest run` 2830/2830, `vue-tsc --noEmit` limpio. 3 capturas reales via CDP (`Page.captureScreenshot` directo, no una pagina Vite suelta) en `docs/reference/qa-evidencia/qa102-comandos-estado{1,2,3}-*.png`. `[QA-102]`

## revision-ux-sesion-real-4 — H2: T-pose al cambiar de avatar (`VrmAvatar.vue`, D7)

- ✅ **Causa raiz real**: `swapModel()` llamaba `resetBodyAnimationState()` ANTES de `loadModel()`, con `vrm`/`mixer` en `null` -- las 4 variables de estado de animacion (`animationAction`, `playingAnimationUrl`, `poseAction`, `playingPoseUrl`) quedaban apuntando al mixer viejo ya destruido, y el guard de `ensureAnimationAction`/`ensurePoseAction` bloqueaba re-crear la accion sobre el mixer nuevo -- el modelo quedaba en bind pose (T-pose). `initialize()` nunca sufria esto porque ya llamaba `loadModel()` primero. Corregido invirtiendo el orden en `swapModel()` para que coincida exactamente con `initialize()` (confirmado linea por linea por `solid-guardian`: PASS, sin violaciones, acoplamiento minimo y bien encapsulado por el `defineExpose({resetPoseForCurrentState: resetBodyAnimationState})` agregado para que el Hito 3 (`/clear`) reuse el mismo reset). `vue-tsc --noEmit` limpio, `vitest run` 2830/2830 sin regresion. Verificado en vivo contra la app real: cambio de personaje en caliente (`window.__qaSelectCharacter('rabbit')`) sin mandar mensaje ni tocar modo mascota, avatar con brazos en pose de reposo natural -- 2 capturas reales via CDP en `docs/reference/qa-evidencia/qa103-avatar-*.png`. **Brecha declarada**: sin spec unitario nuevo para el componente Vue (ningun `.vue` del proyecto tiene spec propio, exencion `presentacion-ui` ya establecida; la logica pura ya vive en modulos `.ts` testeados aparte). `[QA-103]`

## revision-ux-sesion-real-4 — H3: `/clear` real con confirmacion (`App.vue`, `clear-command.ts`, `AvatarStage.vue`, D2 revisada)

- ✅ **Rediseño en vivo tras una prueba que invalido el enfoque original**: se iba a implementar `/clear` cerrando y reabriendo el proceso; una prueba real (Hito 1) demostro que enviarlo como texto normal ya resetea el contexto por si solo (Claude Code emite un evento `conversation_reset` real + hook `SessionStart:clear` + `system/init` con `session_id` nuevo). Implementado: `/clear` se envia igual que `/effort` (mismo `sendChatMessage`), con dialogo de confirmacion previo (`role="alertdialog"`, foco atrapado, mismo patron ya usado por otros overlays del archivo) interceptado en `commitSelectedCommand()`. `isClearSessionReset(previousSessionId, newSessionId)` (`clear-command.ts`, funcion pura con 3 pruebas) decide si un `session_started` cuenta como confirmacion real de `/clear` o es un arranque normal -- si aplica, vacia `chatState`/`activityConsole` e invoca `resetPoseForCurrentState()` (expuesto por `AvatarStage.vue`, reenviando el metodo del Hito 2).
- ✅ **`design-critic` encontro y se corrigio en el mismo hito un hallazgo CRITICO real**: el boton "Confirmar" (accion destructiva/irreversible) era visualmente identico a "Cancelar" -- no usaba la clase `session-panel__button--danger` ya existente en el proyecto para exactamente este caso. Corregido agregando la clase y un `aria-describedby` faltante. `solid-guardian`: PASS, con 2 hallazgos menores no bloqueantes ya mitigados por el ciclo de vida de Vue.
- ✅ **Verificado en vivo end-to-end contra la app real, con conversacion real previa (no una sesion recien arrancada)**: sesion con contexto real (5 mensajes) -> seleccionar `/clear` -> confirmar -> `session_id` nuevo real emitido por Claude Code, `chatMessages` paso de 5 a 0, avatar sin T-pose tras el reset. Tambien se confirmo el caso borde real: probar `/clear` en una sesion sin ningun mensaje previo (sin `session_id` real aun) correctamente NO dispara el reset visual, por diseño (`previousSessionId` null no cuenta como confirmacion) -- comportamiento correcto, no un bug. Cancelar el dialogo verificado sin ningun efecto secundario (sessionId y mensajes identicos a antes de abrirlo). 2 capturas reales via CDP en `docs/reference/qa-evidencia/qa104-clear-*.png`. **Brecha declarada**: el relleno rojo del boton `--danger` solo se distingue en `:hover` (limitacion del boton compartido en toda la app, no una regresion de este hito) -- se deja declarada para una revision transversal futura si se decide abordarla. `[QA-104]`

## revision-ux-sesion-real-4 — H4: sesion, cierre automatico de la anterior + rollback real (`App.vue`, `session-start-retry.ts`, D3)

- ✅ **Causa raiz real**: `resumeSession`/`beginSession` mutaban `chosenCwd`/`sessionId` de forma optimista ANTES de saber si el arranque tendria exito; si el backend rechazaba por sesion ya activa, el `catch` solo fijaba `startError` sin revertir nada -- el menu nativo "Sesion" quedaba deshabilitado para siempre. Corregido con `startSessionWithRetry()` (`session-start-retry.ts`, modulo puro, 7 pruebas): reintenta el arranque una vez cerrando la sesion vieja SOLO si el error es de sesion ya activa, y `beginSession`/`resumeSession` ahora guardan `previousCwd`/`previousSessionId` antes de mutar y revierten a esos valores en cualquier fallo definitivo. Se eliminaron los botones manuales "Reconectar sin cerrar"/"Cerrar sesion anterior" y el ref `startErrorIsStaleSession`, que quedaban contradictorios con el reintento automatico transparente que exige D3 -- confirmado sin colision con TC-015 (usa un boton distinto) ni codigo colgante (grep limpio de los 4 nombres eliminados). `solid-guardian`: PASS, rollback confirmado completo en los 4 caminos posibles (exito directo, retry exitoso, retry fallido, error no relacionado).
- ✅ **Verificado en vivo end-to-end contra la app real**: abrir sesion A, luego abrir sesion B sin cerrar A manualmente -- B queda activa con `startError:null` (antes de este hito: `sessionActive:false`, `startError:'ya hay una sesion activa'`, permanente), mensaje real enviado y respondido con exito en la sesion B resultante. `vitest run` 2840/2840, `npm run build` limpio. Captura real via CDP en `docs/reference/qa-evidencia/qa105-sesion-b-tras-retry-transparente.png`. `[QA-105]`

## revision-ux-sesion-real-4 — H5: resembrar la consola de Actividad al reanudar (`session_history.rs`, `activity-console.ts`, `App.vue`, D4)

- ✅ **Causa raiz real**: `resumeSession` ya resembraba `chatState` desde el transcript, pero `activityConsole` nunca se tocaba (la unica asignacion real ocurria en eventos EN VIVO). Corregido con un comando Rust nuevo `read_session_activity_events` que normaliza el mismo `.jsonl` con el MISMO `event_normalizer::normalize` que usa el streaming en vivo, y `resumeActivityConsoleState()` (TS) que reutiliza el mismo reductor `handleNormalizedEvent` -- sin duplicar logica de interpretacion. `solid-guardian`: OBSERVACIONES (no bloqueante) -- desviacion aceptada de D4 (dos lectores del `.jsonl` en vez de uno, justificada porque `ClaudeEvent` no captura texto de usuario) y un hallazgo real SI corregido en el mismo hito (errores de lectura de historial silenciados con `.catch(() => [])`, ahora con `console.error` descriptivo).
- ✅ **Verificado en vivo contra un archivo real de disco** (no un fixture sintetico): reanudar la sesion mas reciente de este mismo proyecto (437 mensajes reales de una sesion de trabajo anterior) puebla tanto el chat COMO la consola de Actividad con bloques reales (Lectura, Exito, Modificacion) de inmediato, sin esperar ningun evento nuevo -- antes de este hito la consola habria quedado vacia. `vitest run` 2842/2842, `cargo test` 134/134. Captura real via CDP en `docs/reference/qa-evidencia/qa106-actividad-resembrada-tras-reanudar.png`. **Brecha declarada**: el registro de error de lectura fallida es solo tecnico (consola de DevTools), sin aviso visible en la UI -- se deja para una iteracion futura si se reporta como problema real. `[QA-106]`

## revision-ux-sesion-real-4 — H6: Markdown real en el chat y en la vista Markdown de Actividad (`content-interpreter.ts`, `ContentBlockRenderer.vue`, `App.vue`, `external-link-click.ts`, D5)

- ✅ **El ejemplo exacto reportado por el usuario ya renderiza bien**: "El **Plan mode** permite planear sin tocar codigo" ahora muestra `<strong>Plan mode</strong>` real, sin asteriscos visibles. Solo el bloque `paragraph` pasa por `markdown-it` (reusando la MISMA instancia ya usada en el preview del composer); los 9 tipos de bloque estructural (encabezado, codigo, diff, referencia de archivo, comando, tabla, ascii-art, warning, error) confirmados intactos por `solid-guardian` linea por linea, ninguno usa `v-html`.
- ✅ **`design-critic` encontro y se corrigieron en el mismo hito 2 hallazgos CRITICOS reales**: (1) los enlaces generados por markdown no tenian tema en 2 de 3 superficies (caian al azul nativo del navegador, rompiendo `theme-first`) -- corregido con `:deep(a)` en las 3 superficies, con un matiz real detectado por el propio auditor (la burbuja de usuario necesita `--color-text-primary` + subrayado en vez de `--color-accent-primary`, porque el fondo de esa burbuja ya es ese mismo matiz y daria bajo contraste); (2) un clic en un enlace dentro del webview de Tauri podia navegar la app entera fuera o quedar bloqueado por CSP, sin ningun manejo -- nuevo modulo `external-link-click.ts` (2 pruebas unitarias) que intercepta el clic y abre la URL con `@tauri-apps/plugin-opener` (dependencia YA instalada, permiso `opener:default` YA concedido, cero dependencia nueva agregada).
- ✅ **Verificado en vivo contra la app real**: `vitest run` 2847/2847, `vue-tsc --noEmit` limpio. 4 capturas reales via CDP en `docs/reference/qa-evidencia/qa107-*.png` confirmando negrita real, enlace con tema y subrayado, y lista con sangria real (`:deep(ul)`/`:deep(ol)` agregado como corrección adicional de bajo costo). **Brechas declaradas**: tablas/blockquote escritos a mano dentro de un mensaje no tienen el mismo tratamiento visual que las tablas estructurales (fuera de alcance, no pedido en el reporte original); el arbol de encabezados `h1`-`h6` generados dentro de una burbuja de chat es un comportamiento **preexistente** (ya generaba encabezados reales antes de este hito), no una regresion introducida aqui — se deja declarado para una revision de accesibilidad transversal futura.
- ✅ **Corrección posterior (Hito 15, hallazgo real de un `fresh-verifier` independiente)**: el criterio literal de `spec.md` ("se lee en voz alta sin los caracteres de marcado") NO estaba cubierto por este cierre original -- `blockToSpeechText()` (`content-block-text.ts`) devolvía `block.text` crudo para bloques `paragraph`, con `**`/`#` intactos, llegando así hasta el motor de TTS real (confirmado el camino completo: `App.vue` → `reaction-catalog.ts` → `reaction-engine.ts` → `text-to-speech.ts`, sin ningún paso de limpieza en medio). Corregido con `stripMarkdownForSpeech()`, que reusa la MISMA instancia de `markdown-it` (vía `renderChatDraftMarkdown`) y descarta solo las etiquetas que el propio renderer genera -- sin sanitizar HTML arbitrario ni reimplementar reglas de Markdown por regex, aplicado únicamente al tipo `paragraph`. Prueba unitaria adversarial nueva con negrita+encabezado+lista reales, `vitest run` 562/562 tras el fix. `[QA-107]`

## revision-ux-sesion-real-4 — H7: boton flotante de toggle Markdown/crudo por mensaje (`App.vue`, `ContentBlockRenderer.vue`, D6)

- ✅ **`solid-guardian` encontro y se corrigio un bug real en el mismo hito**: `rawViewEntryIds` (el `Set` de ids en modo crudo) nunca se limpiaba en los 2 puntos donde `chatState` si se resetea (`/clear` del Hito 3, reanudar sesion del Hito 5) -- escenario real: togglear un mensaje a modo crudo, hacer `/clear`, y el mensaje NUEVO que cae en el mismo indice aparecia en modo crudo sin que nadie lo pidiera. Corregido sincronizando el reset en ambos puntos.
- ✅ **`design-critic` encontro y se corrigio un CRITICO real**: el `padding-right` de la burbuja (32px) no alcanzaba para el boton flotante real (~30px + 8px de offset = 38px), tapando ~6px de texto en mensajes que llenan el ancho de la burbuja -- corregido a 40px con la formula exacta que dio el propio auditor.
- ✅ **Verificado en vivo contra la app real**: `vitest run` 3380/3380 (0 fallos reales, conteo inflado por sandboxes viejos de Stryker, ruido preexistente), `vue-tsc --noEmit` limpio. Boton con `aria-pressed` dinamico, click alterna entre Markdown renderizado y texto fuente con asteriscos literales visibles, sin tapar texto real. 3 capturas reales via CDP en `docs/reference/qa-evidencia/qa108-*.png`. **Brechas declaradas**: no se migro de indices de array a ids estables en `ChatEntry` (riesgo bajo en la practica actual, deuda tecnica declarada para si el chat agrega busqueda/filtrado); objetivo tactil del boton (~30x30px) por debajo del ideal 44x44 aunque cumple el minimo WCAG. `[QA-108]`

## revision-ux-sesion-real-4 — H8: mascota, posicion persistente + boton restaurar + panel de configuracion (`app_settings.rs`, `PetView.vue`, `presentation-manager.ts`, `App.vue`, D8)

- ✅ **Descubrimiento arquitectonico real durante la implementacion**: el arrastre de la mascota ocurre en `PetView.vue`, una VENTANA TAURI SEPARADA (proceso distinto, sin memoria compartida) -- `startWindowDrag()` en `presentation-manager.ts` (donde el plan original pedia capturar la posicion) resulto estar muerto en produccion (confirmado por `solid-guardian` via grep: solo lo usa su propio spec). La persistencia real se movio a `PetView.vue`, con `App.vue::switchToPetMode()` releyendo `AppSettings` de disco antes de cada activacion (necesario por ser procesos separados). `solid-guardian`: PASS.
- ✅ **Verificado en vivo con la ventana PET real, no un mock**: se escribio una posicion de prueba directo al archivo de configuracion, se activo el modo mascota real (aparecio una SEGUNDA ventana Tauri real, CDP target separado con `label:'pet'`), y su posicion nativa (`outer_position()`) coincidio exactamente con lo persistido. Se confirmo el ciclo completo con "Restaurar posicion": el archivo real vuelve a `null` y el panel muestra "Default" de inmediato. Limite de tamaño confirmado en el DOM real (antes 400px, ahora 800px). `cargo test` 136/136, `vitest run` 3393/3393, `cargo clippy` sin warnings. 3 capturas reales en `docs/reference/qa-evidencia/qa109-*.png` (incluida una captura de la propia ventana PET, no solo la principal). **Brechas declaradas**: `startWindowDrag()` queda como codigo inerte documentado (no se elimino para no tocar sus pruebas existentes ni ampliar alcance); el limite de 800px no se confirmo contra un monitor de referencia real (supuesto sin resolver). `[QA-109]`

## revision-ux-sesion-real-4 — H9: autenticacion de MCP/plugin, verificacion real del resultado (`claude_admin.rs`, `mcp-auth-outcome.ts`, `AdminSettingsPanel.vue`, D9)

- ✅ **Verificado en vivo contra el caso EXACTO del reporte original del usuario**: el servidor real `plugin:figma:figma` de este mismo proyecto (status real `needs-auth`) se autentico desde el panel real -- ya no dice falsamente "se abrio el navegador". Ahora captura y muestra el error REAL del proceso (`Stdio::piped()` en vez de `Stdio::null()`, con `tokio::join!` sobre `child.wait()` + lectura de stdout/stderr dentro de un timeout de 4s, sin riesgo de deadlock confirmado por `solid-guardian` linea por linea): `"stdin isn't a terminal, so authentication can't be completed here..."` -- **esto revela la causa raiz definitiva del Bug 4**: el login de MCP de Claude Code SIEMPRE requiere TTY interactivo, asi que NINGUN servidor con auth interactiva puede completarse desde este panel sin usuario manual fuera de la app. Ademas se muestra el aviso nuevo para namespaces `plugin:*` ("depende del plugin que lo publica").
- ✅ **`solid-guardian`: OBSERVACIONES no bloqueante**: unico hallazgo real (stderr sin sanitizar antes de mostrarse) evaluado como riesgo bajo y consistente con el patron ya existente en el resto del archivo -- no una regresion de este hito. `cargo test` 28/28, `vitest run` 3400/3400, `clippy`/`fmt` limpios. Captura real via CDP en `docs/reference/qa-evidencia/qa110-mcp-auth-error-real-figma.png`. **Brecha declarada**: sin sanitizacion de stderr (riesgo bajo, consistente con el resto del proyecto); la limitacion de TTY es del propio Claude Code, no algo que la app pueda resolver -- el hito cumple dejar de mentir sobre el resultado, no promete completar el login. `[QA-110]`

## revision-ux-sesion-real-4 — H10: `CustomSelect.vue` reusable + Nivel de Esfuerzo al chat (`App.vue`, `CustomSelect.vue`, D10/D11)

- ✅ **`solid-guardian`: OBSERVACIONES no bloqueante** (mezcla de modo inline/trigger en un componente, riesgo a vigilar en el Hito 11 -- sin bug actual, aclarado con comentario en vez de forzar una division en 2 componentes por YAGNI) + **`design-critic` encontro y se corrigieron en el mismo hito 2 CRITICOS reales, medidos contra los tokens de la app real**: el panel del selector de Nivel de Esfuerzo siempre abria hacia abajo sin importar el espacio disponible (se cortaba contra el borde de la ventana o tapaba el composer) -- corregido a abrir hacia arriba para ese placement; y el control no tenia identidad visual propia, se confundia con una burbuja de chat -- corregido con fondo/borde propio.
- ✅ **Verificado en vivo con medicion real de posiciones** (no una suposicion visual): `panelBottom(897) &lt;= triggerTop(905)` confirma apertura hacia arriba, `composerTop(958)` muy por debajo confirma que no tapa la caja de texto. Listado de comandos migrado al componente nuevo sin ninguna regresion de los Hitos 1/3 (mismo estado vacio exacto que antes). `vitest run` 3400/3400, `vue-tsc --noEmit` limpio. Captura real en `docs/reference/qa-evidencia/qa111-nivel-esfuerzo-panel-hacia-arriba.png`. **Brechas declaradas**: sin division en 2 componentes (documentado con comentario, se revisita si el Hito 11 lo necesita realmente); labels de esfuerzo sin traducir (`low`/`medium`/etc.) es intencional -- son flags literales del CLI. `[QA-111]`

## revision-ux-sesion-real-4 — H11: migrar los 9 `<select>` restantes a `CustomSelect.vue` (`CharacterEditor.vue`, `DebugPanel.vue`, `PoseEditor.vue`, `SessionStartOptionsPanel.vue`, `VoiceControls.vue`)

- ✅ **`solid-guardian`: PASS** -- confirmado que `disabled` real bloquea la apertura del trigger (boton HTML nativo), `title`/`aria-label` con la razon real llegan al DOM; sustitucion de `<optgroup>` por prefijo de texto evaluada como aceptable (limitacion ya conocida desde el Hito 10); cero `<select>` nativos remanentes, cero cambios de contrato hacia los componentes padre. **`design-critic` encontro y se corrigio en el mismo hito un CRITICO real**: el trigger no truncaba texto largo (a diferencia del `<select>` nativo que reemplazaba), asi que un nombre de animacion/pose importado largo desbordaba el boton y rompia el panel -- corregido con `max-width`+`ellipsis`.
- ✅ **Verificado en vivo con datos reales del catalogo del proyecto**: 36 triggers reales renderizados en el Editor de personaje (5 selects x 6 estados + esquina + voz + esfuerzo), incluyendo un label real largo (`"Microsoft Sabina - Spanish (Mexico) (es-MX)"`) sin desbordar. `vitest run` 3400/3400, `vue-tsc --noEmit` limpio. Captura real en `docs/reference/qa-evidencia/qa112-editor-personaje-selects-migrados.png`. **Brechas declaradas**: perdida del anuncio de grupo `<optgroup>` para lectores de pantalla (limitacion conocida, compensada parcialmente); estado `disabled` del trigger con distincion visual floja (patron preexistente del boton compartido, no introducido por este hito). `[QA-112]`

## revision-ux-sesion-real-4 — H12: indicador de actividad en tiempo real + prefijo `$` condicional (`chat.ts`, `App.vue`, `activity-console.ts`, `content-interpreter.ts`, `ContentBlockRenderer.vue`, D12)

- ✅ **`solid-guardian`: PASS** -- `isShellCommand` consistente en los 3 puntos reales de construccion de bloques `command`, prefijo `$` condicional correcto. **`design-critic` encontro un CRITICO real distinto**: mientras se enviaba un mensaje, `chatEntries()` seguia agregando una entrada vacia del turno abierto AL MISMO TIEMPO que el indicador "Trabajando" se mostraba por separado -- dos `<li>` compitiendo por el mismo turno, uno de ellos una burbuja vacia con un boton huerfano. Corregido en el mismo hito re-arquitecturando a un unico `<li>` por entrada (el texto "Trabajando" vive DENTRO de la misma burbuja del turno abierto, nunca en un nodo aparte) -- confirmado por grep que solo queda un bloque de plantilla. Tambien corregido el uso indebido de un token de color reservado a un status-label chico, aplicado sobre texto completo.
- ✅ **Verificado en vivo con 2 mensajes reales** a una sesion real, ademas de la garantia estructural (un solo `<li>` posible por diseño de la plantilla, no por casualidad de timing): ninguna entrada duplicada ni burbuja vacia residual tras la respuesta. `vitest run` 556/556 -- numero real y limpio tras remover 7 sandboxes obsoletos de Stryker que estaban causando falsos positivos en el hook de cierre (confirmado que las fallas reportadas eran instantaneas congeladas de un sandbox viejo, no del codigo real). `vue-tsc --noEmit` limpio. Captura real en `docs/reference/qa-evidencia/qa113-chat-sin-burbuja-fantasma.png`. **Brecha declarada**: no se capturo el instante exacto (unos cientos de ms) del indicador visible durante un envio real por la latencia normal de respuesta -- resuelto por garantia estructural equivalente en vez de una captura del instante transitorio. `[QA-113]`

## revision-ux-sesion-real-4 — H13: ultimas 5 sesiones de cualquier carpeta sobre la cuadricula de carpetas (`SessionHistoryExplorer.vue`, `session-history-format.ts`, D13)

- ✅ **`solid-guardian`: OBSERVACIONES** -- `pickRecentSessionsAcrossFolders` con responsabilidad unica, `sortSessionsByDateDescending` generalizada a `<T extends SessionSummary>` sin romper usos previos, carga no bloquea la cuadricula de carpetas. Unico hallazgo real: `loadFolders()` y `loadRecentSessions()` llamaban `listProjectFolders()` por separado -- corregido encadenando `loadFolders().then(loadRecentSessions)` para compartir el resultado. **`design-critic` encontro 2 CRITICOs reales**: (1) el titulo nuevo era `<h3>` saltando el `<h2>` esperado justo bajo un `<h1>` sin nivel intermedio, rompiendo ademas el patron de encabezados del resto de `App.vue` -- corregido a `<h2>`; (2) la misma query duplicada generaba una carrera visible (la cuadricula de carpetas, una sola llamada, resolvia antes que "recientes", N llamadas) empujando el layout hacia abajo -- mitigado con el mismo fix de query compartida. Se corrigio ademas, por ser de costo trivial, una OBSERVACION real: el meta de cada item de "recientes" no mostraba costo/duracion como si lo hace la lista por-carpeta -- agregado. Quedan sin resolver por YAGNI dos OBSERVACIONES sin defecto funcional: asimetria de encabezado con la cuadricula de carpetas, y duplicacion de markup entre los dos tipos de item de sesion (ya comparten tipo de datos, falta solo la abstraccion de presentacion).
- ✅ **Verificado en vivo contra las 46 carpetas reales de proyecto de este equipo**: el orden de las 5 sesiones mostradas coincide exactamente, id por id, con ordenar manualmente por `startTimeMs` real el resultado crudo de `list_project_folders`/`list_project_sessions` invocados directamente. Click real en "Reanudar" sobre un item de "recientes" (sesion de $3.42) confirmo que `qaState().sessionId` resulto igual al id de esa tarjeta -- reusa el camino real de H4 sin logica de arranque duplicada. `vue-tsc --noEmit` limpio, `vitest run` 559/559, `npm run build` OK. Capturas reales en `docs/reference/qa-evidencia/qa114-recientes-multi-carpeta-angulo1-render.png` y `qa114-recientes-angulo3-reanudar-real.png`. **Brechas declaradas**: las 2 OBSERVACIONES de diseño no corregidas (ver arriba); detectado incidentalmente que `__qaCloseSessionFromMenu()` devuelve `session-not-active` para una sesion abierta desde "recientes" en vez del menu nativo -- fuera del alcance de este hito y de `spec.md`, queda documentado para una ronda futura. `[QA-114]`

## revision-ux-sesion-real-4 — H14: panel de uso, tokens/costo reales de la sesion + boton "Ver detalle de plan" (`event_normalizer.rs`, `claude-transport.ts`, `chat.ts`, `App.vue`, D14)

- ✅ **`solid-guardian`: PASS** -- `normalize_result()` sigue pura/sincrona, tolera campos ausentes de `usage` con `unwrap_or(0)`; `accumulateSessionUsage()` inmutable, mismo estilo funcional del resto de `chat.ts`; reset correcto en `resumeSession()`/`closeActiveSession()`; reutiliza `formatSessionCost`/`formatTokenCount` y el mismo carril caliente de `/effort`/`/clear`. **`design-critic` encontro 2 CRITICOs reales**: `session-usage-panel` y `chat-effort-control` compartian literalmente el mismo bloque de estilos (mismo padding/border-top/background), leyendose como la misma fila repetida sin distinguir control interactivo de dato de solo lectura -- corregido con `--color-surface-raised`, sin `border-top` duplicado y `--font-weight-bold` en el valor; los numeros (el dato que el usuario vino a leer) pesaban menos tipograficamente que su propia etiqueta -- corregido igualando tamaño con negrita. Se corrigio ademas un hallazgo real IMPORTANTE: si `sendChatMessage()` fallaba al enviar `/usage`, el modal quedaba en "Enviando /usage..." para siempre -- separado en un mensaje de error explicito.
- ✅ **Verificado en vivo contra un proceso `claude -p` real de este proyecto**: un turno real ("di ok") con `total_cost_usd:0.1437386`/`input_tokens:2` en el evento crudo coincidio exactamente con "Entrada: 2 tokens"/"Costo: $0.14" en el panel; click real en "Ver detalle de plan" mostro la respuesta real y verbatim de `/usage` (sin parseo de porcentajes); reanudar una sesion real DISTINTA desde "recientes" (H13) reinicio el panel a cero de inmediato, confirmando el reset end-to-end contra el proceso real. `cargo build`/`cargo test` 141/141, `vue-tsc --noEmit` limpio, `vitest run` 561/561. Capturas reales en `docs/reference/qa-evidencia/qa115-panel-uso-angulo{1,2,3}-*.png`. **Brechas declaradas**: no se probo acumulacion a traves de multiples turnos reales consecutivos por disciplina de costo (cubierto por pruebas unitarias reales); no se forzo una falla real de red para el nuevo mensaje de error del modal (corregido por inspeccion de codigo tras el hallazgo de `design-critic`).
- ✅ **Corrección posterior (Hito 15, hallazgo real de un `fresh-verifier` independiente)**: el panel acumulaba correctamente los tokens de cache (`cacheCreationInputTokens`/`cacheReadInputTokens`) pero nunca los mostraba, pese a que `spec.md` exige literalmente "tokens de entrada/salida/cache y costo". Corregido agregando un stat `Cache:` (suma de creación+lectura). Verificado en vivo con un turno real nuevo: panel mostró "Cache: 55.2K tokens" junto al resto de datos reales de la misma sesión -- captura en `docs/reference/qa-evidencia/qa115-panel-uso-angulo4-cache-tokens.png`. `vitest run` 562/562 tras el fix. `[QA-115]`

## revision-ux-sesion-real-4 — H15: QA final, cierre transversal del plan

- ✅ **9 `fresh-verifier` independientes en paralelo** sobre H1, H2, H3, H4, H6, H8, H9, H14 (los de mayor riesgo declarados en el plan) más un verificador general de consistencia del plan completo. 7 de 9 **CONFIRMADOS** sin hallazgos bloqueantes (H1, H2, H3, H4, H8, H9, más el general en 4 de sus 5 puntos). **2 REFUTADOS reales, ambos corregidos en este mismo cierre**: (1) H6 no cubría el criterio literal de voz de `spec.md` ("se lee en voz alta sin los caracteres de marcado") — `blockToSpeechText()` devolvía Markdown crudo (`**`/`#`) directo al motor de TTS; corregido con `stripMarkdownForSpeech()` (`content-block-text.ts`), que reusa la MISMA instancia de `markdown-it` de H6 en vez de reimplementar reglas de Markdown por regex; (2) H14 acumulaba tokens de cache pero nunca los mostraba en el panel — corregido agregando el stat `Cache:`. El verificador general encontró además, correctamente, que el fix de voz estaba a medio hacer en el momento de su revisión (roto por un `DOMParser` no disponible en el entorno de pruebas `node`) — corregido reemplazando el `DOMParser` por un strip de las propias etiquetas HTML que `markdown-it` genera (sin sanitizar HTML arbitrario de terceros).
- ✅ **Catálogo combinatorio de flujo cruzado** construido directamente sobre los flujos reales de `plan.md` (all-transitions + round-trip como criterio explícito), sin forzar el enganche a la capa de documentación de negocio `flujo_projects/codetuver-avatar/` porque esta ronda de bugfix ad-hoc no tiene una User Story formal ahí (confirmado por grep). El caso de mayor riesgo — interacción cruzada H3+H7+H14 en una sola acción real — se ejecutó en vivo contra un proceso `claude -p` real: sesión con costo real acumulado ($0.25) + toggle de vista cruda activo → `/clear` real completo (tecleo incremental para abrir el listado real, confirmación real, click en Confirmar) → chat, toggle de vista cruda y panel de uso volvieron a cero **en la misma pasada**, con el evento crudo real (`conversation_reset` + hook `SessionStart:clear` + `session_id` nuevo) confirmando el reset real del proceso. Capturas en `docs/reference/qa-evidencia/qa116-flujo-cruzado-clear-*.png`. **Hallazgo real declarado** (no corregido por YAGNI, edge case de baja probabilidad): si el texto `/clear` completo llega de una sola vez al campo (pegado desde portapapeles, dictado por voz) en vez de tecleado caracter por caracter, el listado inline real nunca se abre y el Enter subsiguiente ejecuta `/clear` sin pasar por la confirmación — declarado en `QA-104` y `QA-116`.
- ✅ **Recuento real final** (herramienta real, sin arrastrar cifras de párrafos anteriores): `cargo test` en `src-tauri/` → **141/141**. `npx vitest run --exclude "**/.stryker-tmp/**"` → **562/562** (49 archivos). `npx vue-tsc --noEmit` → limpio. `[QA-116]`

## revision-ux-sesion-real-5 — H1 a H16: implementación de los 6 bugs, 3 mejoras y 1 agregado del plan, con verificación en vivo contra el proceso real (CDP puerto 9222, `window.__TAURI_INTERNALS__` confirmado)

- ✅ **H2 — "Cerrar sesión" refleja la capacidad real de cerrar** (`native-menu.ts`, `App.vue`, D14): `setSessionInfo` ganó un tercer parámetro `closable` derivado de `sessionActive`, no de `sessionId`. `fresh-verifier` confirmó el código (`vue-tsc`/`native-menu.spec.ts` 51/51). Verificado en vivo: `__qaBeginSession` deja `sessionActive=true`/`sessionId=null`, y `__qaCloseSessionFromMenu()` en ese instante exacto cierra la sesión de inmediato — el bug original reproducido y confirmado corregido contra el proceso real. `[QA-117]`
- ✅ **H1/H7/H9/H10/H12 — auto-scroll de comandos, esfuerzo por ícono, popup de detalle de plan legible, botón compacto**: sesión real de punta a punta (primer `stdin` real vía `__qaSend`, respuesta real del agente). H1: ítem resaltado permanece visible tras 10 flechas (medido con `getBoundingClientRect`). H7: las 5 opciones reales de esfuerzo, selección aplica y envía `/effort high` sin paso intermedio, confirmación real recibida. H9/H10: popup con color real `oklch(0.96 0 0)` (no negro), ícono de cerrar real, trigger reubicado al header del chat. H12: ícono del toggle mide 14×14px reales (`--size-icon-sm`). `[QA-118]`
- ✅ **H3/H4/H5 — comando crudo interpretado, carpetas legibles, ícono real de Claude**: `fresh-verifier` confirmó el código (`parseCommandInvocationTags`, 113 pruebas relevantes). Verificado contra **más de 40 sesiones reales de este equipo** (`~/.claude/projects/`): títulos como `/clear`, `/model`, `/plugin`, `/effort`, `/context`, `/flujo-core:workflow-plan` renderizan limpios en vez del XML crudo original, para sesiones reales cuyo primer mensaje fue exactamente ese patrón. Carpeta real mostrada truncada y legible (`C:\Users\...\Documents\VtuberXD`). Sesión real de 1298 líneas reanudada exitosamente. `[QA-119]`
- ✅ **H11/H13 — auto-scroll y ventaneo del chat, con un bug real encontrado y corregido en esta misma pasada**: la verificación EN VIVO (no la lectura estática de `fresh-verifier`, que había dado CONFIRMADO) encontró que el `IntersectionObserver` nunca se armaba en el uso real (vivía en un `onMounted` del componente raíz, que corre antes de que exista sesión activa) y que `revealMoreEntries()` podía reducir `visibleEntryCount` a 0 si el centinela disparaba con la lista vacía — ambos corregidos (`watch` reactivo sobre las refs del DOM real; `Math.max` en `revealMoreEntries`), con prueba de regresión nueva y reverificación en vivo: 30 turnos reales inyectados vía `__qaInjectEvent`, 24 renderizados al fondo exacto, scroll hacia arriba revela el resto compensando la posición visual sin saltar. `[QA-120]`
- ✅ **H6 — render VRM real de personajes importados**, el hito de mayor riesgo del plan: se importó el archivo real `MegaAngel.vrm` (el mismo que el usuario intentó importar originalmente) y se activó dentro de una sesión real — el modelo 3D real (personaje alado con halo) renderizó en el canvas de `three-vrm`, sin placeholder ni error. Asset protocol (`tauri.conf.json`) y feature `protocol-asset` (`Cargo.toml`) confirmados funcionando de punta a punta contra el proceso real, no solo en `cargo check`. **Brecha declarada**: no se repitió con `GURRRI.glb` ni se forzó un archivo corrupto para ver el mensaje de error en vivo (confirmado solo por lectura estática del mecanismo reusado de `VrmAvatar.vue`). `[QA-121]`
- ✅ **H14/H15 — blur de superficies de listado y tono de voz**: `--blur-panel` mide `32px` real en runtime (no solo declarado en `constants.css`). Panel de voz real muestra los 3 controles (Volumen/Velocidad/Tono) y la voz real detectada por el sistema (`Microsoft Sabina - Spanish (Mexico)`, coincide con la investigación del ADR-0013, ya actualizado). `[QA-122]`
- ✅ **H8 — comandos en caliente nunca se narran**: confirmado por lectura de código (`awaitingEffortResponse`/`awaitingUsageDetail`/`awaitingClearReset` leídos antes de `reactToEvent` en `session_finished`) y por `solid-guardian` (PASS). **Brecha declarada**: confirmar audialmente que no hay narración exige reproducción de audio real (criterio que depende de oído humano, no de un assert automatizable — ver memoria `verificacion-sin-hardware`); no se verificó en vivo en esta pasada. `[QA-123]`
- ✅ **H16 — auditoría general de popups**: los 4 overlays de `App.vue` comparten `.commands-overlay__panel` (ya corregido y confirmado en vivo en `QA-118`); `DebugPanel.vue`/`EditorModal.vue` corregidos por lectura de código (`Teleport to="body"` de `EditorModal` escapa la herencia de `#app` por completo). **Brecha declarada**: el intento de abrir en vivo el `EditorModal` del editor de posado no completó en esta pasada (elemento fuera de vista al automatizar el click); no es un hallazgo real, solo evidencia pendiente. `[QA-124]`
- ✅ **`solid-guardian` sobre el diff acumulado completo de los 16 hitos: PASS**, sin violaciones — funciones puras extraídas de forma consistente (`chat-scroll.ts`, `chat-history-window.ts`, `avatar-imported-model.ts`), inyección de dependencias (`convertFileSrc` inyectado, no importado directo), índices reales preservados tras el recorte del ventaneo.

## compatibilidad-personajes-importados — H1: animación y pose reales para personajes VRM importados

- ✅ **H1 — `capabilitiesForImportedCharacter()` habilita animación/pose para VRM importado, Live2D sigue bloqueado**: `solid-guardian` PASS (firma nueva de 2 parámetros, sin call-sites huérfanos). Verificado en vivo contra el proceso real (CDP puerto 9222, `__TAURI_INTERNALS__` confirmado): se importó `MegaAngel.vrm` real, se activó dentro de una sesión real y `__debugAvatarState()` mostró `hasAction:true`/`actionRunning:true` con `playingAnimationUrl:"neutral-animation-3"` y `poseUrl:"neutral-pose-1"`/`poseActionRunning:true` — ya no fijo en T-pose (regresión de `QA-121` cerrada). El Editor de personaje mostró la fila `idle` con `Animaciones` (`neutral-animation-1/2/3`) y `Pose` (`Fabrica: neutral-pose-1`) editables, sin ningún banner de "deshabilitado". Pulsar "Ejecutar" sobre `neutral-animation-2` previsualizó exactamente esa animación (`playingAnimationUrl` cambió en vivo). **Regresión Live2D confirmada**: se importó el fixture real `personaje-sin-boca.model3.json`, y su Editor mostró "Animacion deshabilitada"/"Pose deshabilitada" con la razón real, más el aviso "Personaje importado (Live2D): sin renderizador todavia, EPIC-004 futuro" en el avatar — sin cambio de comportamiento. Capturas en `docs/reference/qa-evidencia/qa125-h1-*.png` (5 archivos: importación, avatar sin T-pose, editor con animación/pose habilitadas, preview de animación específica, Live2D bloqueado). `npx vue-tsc --noEmit`, `npx vitest run` (1149/1149), `npm run build` limpios. `[QA-125]`

## correcciones-qa-gauntlet — H1: `Escape` en `CustomSelect` no debe cerrar el overlay contenedor

- ✅ **H1 — `CustomSelect.vue::onTriggerKeydown` corta la propagación de `Escape`**: agrega `event.stopPropagation()` junto al `preventDefault()` existente, en el origen compartido por las 12 instancias reales del componente. Sin cobertura unitaria de componente posible (entorno de test `environment:'node'`, sin `@vue/test-utils`/jsdom — ningún `.vue` del proyecto tiene spec propio por el mismo motivo; exención declarada, misma que Hito 4 para la rueda de color). Verificado en vivo contra el proceso real (CDP puerto 9222, `__TAURI_INTERNALS__` confirmado), 3 instancias reales distintas: "Modo destino" y "Capacidad ausente" dentro de `DebugPanel.vue` — un solo `Escape` cierra el select, el panel de depuración permanece abierto en ambos casos; selector de "Nivel de esfuerzo" del composer del chat (instancia sin overlay padre) — `ArrowDown` sigue abriendo/resaltando opciones con normalidad (regresión confirmada) y `Escape` cierra el listado sin afectar el composer. Capturas en `docs/reference/qa-evidencia/qa126-cqg-h1-*.png` (4 archivos). `npx vue-tsc --noEmit` y `npx vitest run` (1149/1149) limpios. **Brecha declarada**: la tercera instancia planificada originalmente ("Esquina" en `CharacterEditor.vue`) se sustituyó por el selector de esfuerzo tras una inconsistencia de medición del DOM (`getBoundingClientRect` fuera de rango de su contenedor scrollable real) atribuible al estado de la sesión en vivo usada, no al fix — no investigada a fondo por no ser parte del criterio de aceptación. `[QA-126]`
- ✅ **H2 — pila `openOverlayStack` reemplaza los 4 listeners de `Escape` independientes de `App.vue`**: `pushOverlay`/`popOverlay` sincronizados en los 4 pares `open*`/`close*` reales (`/clear`, detalle de plan, importar personaje, tema); un único `closeTopOverlayOnEscape` cierra `openOverlayStack[length-1]`. Sin prueba de integración de `App.vue` posible (misma exención de entorno que H1 — ningún `.vue` del proyecto tiene spec propio). `solid-guardian` PASS con 1 hallazgo medio (duplicación de estado) corregido en el momento: las 4 banderas `*Visible` pasaron a `computed` derivadas de la pila. Verificado en vivo contra el proceso real, 5 ángulos: (1) tema+importar abiertos, un `Escape` cierra solo importar (el más reciente), tema permanece; (2) los 4 overlays probados individualmente — cada uno se sigue cerrando con `Escape` igual que antes (sin regresión); (3) cerrar "Detalle de plan" por su propio botón "Cerrar" (no `Escape`), abrir "Tema" — un `Escape` cierra tema (confirma que la pila también se desapila por vías distintas a `Escape`); (4) 3 overlays simultáneos (detalle de plan + tema + importar) — 3 `Escape` consecutivos cierran de más reciente a más antiguo, en orden exacto; (5) spam de 6 `Escape` con la pila ya vacía — sin excepción, sin errores de página. Capturas en `docs/reference/qa-evidencia/qa127-cqg-h2-*.png` (8 archivos). `npx vue-tsc --noEmit` y `npx vitest run` (1149/1149) limpios. `[QA-127]`
- ✅ **H3 — listbox de comandos: cierre por blur y apertura por contenido pegado**: `onChatComposerInput` ya no exige `isSlashCommandTrigger` (`value === '/'` exacto) para abrir el listbox — evalúa `chatDraft.value.startsWith('/')` directo, cubriendo un `paste` de un salto igual que el tecleo incremental (`isSlashCommandTrigger` quedó sin call-sites, eliminada junto a sus 3 pruebas dedicadas). Nuevo `onChatComposerBlur` (`@blur` en el textarea) cierra `commandsListVisible` salvo que el nuevo foco caiga dentro del propio `#commands-listbox`. Verificado en vivo, 3 ángulos + regresión: pegar `/effort` de un solo evento abre el listbox igual que tecleado; `Tab` con el listbox visible lo cierra (foco pasa al siguiente control real); clic en una opción real del listbox la selecciona (`draft` actualizado) sin cierre prematuro por blur (foco permanece en el textarea durante todo el click, ya que las opciones usan `@mousedown.prevent` y nunca roban foco); regresión de tecleo carácter por carácter confirmada sin cambios. Capturas en `docs/reference/qa-evidencia/qa128-cqg-h3-*.png` (5 archivos). `npx vue-tsc --noEmit` y `npx vitest run` (1146/1146, -3 por la eliminación de `isSlashCommandTrigger`) limpios. `[QA-128]`
- ✅ **H4 — paridad de teclado: rueda de color de `ThemePopup` y `Home`/`End` en `CustomSelect`**: `.theme-popup__wheel` (host del SVG de `@jaames/iro`) gana `tabindex="0"`/`role="slider"`/`aria-valuenow` real (sincronizado por `color:change`); `ArrowLeft`/`ArrowRight` mueven el matiz en pasos de 5° y comitean de inmediato (`picker.color.hue` + `onInputEnd` manual, ya que un `keydown` discreto no dispara `input:end` por su cuenta). `CustomSelect.vue::onTriggerKeydown` gana `Home`/`End` (salta a `options[0]`/`options[length-1]`, mismo criterio sin envolver que las flechas) — solo aplica al modo trigger (12 instancias reales); el listbox inline de comandos del composer usa una implementación deliberadamente separada (`to-prove.md` 1.20) y queda fuera de este fix, consistente con el hallazgo original que repro contra el selector de "Nivel de esfuerzo" (modo trigger). Verificado en vivo, 3 ángulos + regresión: `Tab` hasta la rueda + 6× `ArrowRight` cambió el matiz real 14.77°→44.77° (30° exactos) y el color de fondo visible; `Home`/`End` en "Nivel de esfuerzo" saltó a "low"/"max" sin pasar por los de en medio; segunda instancia real distinta ("Modo destino" en `DebugPanel`) con el mismo resultado genérico ("FULL"/"PET"); regresión confirmada — `Tab`/`Shift+Tab` dentro del popup de tema sigue atrapando el foco. Capturas en `docs/reference/qa-evidencia/qa129-cqg-h4-*.png` (6 archivos). `npx vue-tsc --noEmit` y `npx vitest run` (1146/1146) limpios. `[QA-129]`
- ✅ **H5 — clamp de campos numéricos sin tope: Transición (ms) y Rotación X/Y/Z**: `character-editor.ts` gana `clampTransitionDuration` (0-10000ms) y `clampRotationDegrees` (-360 a 360), mismo patrón que `clampCharacterSize`. `CharacterEditor.vue::setTransitionDuration` y `AnimationTimelineEditor.vue::setBoneRotationAxis` (único call-site compartido por los 3 ejes) los usan; `min`/`max` HTML agregados como refuerzo visual. 6 pruebas unitarias nuevas (adversarial primero: valores extremos reales `999999999`/`99999`/`-99999` del propio bug, luego rango válido). Verificado en vivo, 3 ángulos + regresión: "Transición (ms)" con `999999999` clampó a `10000` real (reproducción exacta del bug); "Rotación X" con `99999°` clampó a `360.0`; "Rotación Y" con `-99999°` clampó a `-360.0` (ambos extremos); regresión — "Margen X" con `-500` sigue clampando a `0` (mecanismo `clampAnchorToArea` intacto, no tocado). Nota de proceso: el HMR de Vite no siempre recoge cambios de plantilla/imports en componentes anidados — un `reload()` completo fue necesario antes de que el `max` del input y el clamp real aparecieran en el DOM; sin esto, una verificación en vivo puede dar un falso negativo. Capturas en `docs/reference/qa-evidencia/qa130-cqg-h5-*.png` (3 archivos). `npx vue-tsc --noEmit` y `npx vitest run` (1152/1152, +6) limpios. `[QA-130]`
- ✅ **H6 — `/clear` con texto adicional siempre confirma (bypass de mayor severidad del catálogo, corregido)**: causa raíz real confirmada por lectura — el bypass NO pasaba por `commitSelectedCommand` (esa comparación sólo puede ver el nombre exacto de un comando ya emparejado por el listbox, nunca "clear porfavor" literal); ocurría porque `onComposerEnterKey` no interceptaba nada antes de `sendChatMessage` cuando el listbox no tenía coincidencias para el texto extra. Nueva función pura `isClearCommandWithExtraContent` (`clear-command.ts`) — primer token del mensaje es exactamente `/clear`, excluyendo el `/clear` exacto (ya manejado por el flujo existente) — usada en `onComposerEnterKey` para interceptar con el diálogo de confirmación antes de llegar a `sendChatMessage`. 6 pruebas unitarias nuevas (`/clear porfavor`, `/clear my context please` disparan; `/clear` solo y `/clearly` NO disparan — sin falso positivo ni regresión). Verificado en vivo, 3 ángulos + regresión, reproducción exacta del hallazgo original: `/clear porfavor` + Enter mostró el diálogo sin resetear nada (`sessionId` sin cambios); Confirmar disparó el reset real (`sessionId` nuevo); `/clear algo distinto` + Cancelar no tuvo efecto (`sessionId` sin cambios, diálogo cerrado); regresión — `/clear` solo sigue mostrando el mismo diálogo de siempre. Capturas en `docs/reference/qa-evidencia/qa131-cqg-h6-*.png` (5 archivos). `npx vue-tsc --noEmit` y `npx vitest run` (1158/1158, +6) limpios. `[QA-131]`
- ✅ **H7 — `beginSession` limpia el chat/consola de actividad de la sesión anterior**: `beginSession` (`App.vue`) reinicia `chatState.value`/`activityConsole.value` antes de invocar `startSessionWithRetry`, mismo patrón que la rama `isClearSessionReset` ya existente. `resumeSession` no se toca — función independiente que ya reconstruye desde el transcript real, sin código compartido con `beginSession`. Verificado en vivo, 3 ángulos + regresión, reproducción exacta del hallazgo original: sesión real con 2 mensajes visibles, cerrada (`__qaCloseSessionFromMenu`) y una sesión **nueva** arrancada en la **misma carpeta** — `chatEntryListLength` en `0` en la primera medición tras `beginSession`, ya no arrastra contenido viejo; mismo resultado en una carpeta **distinta**; ciclo `beginSession → cerrar → Retomar (botón real de la UI) → cerrar → beginSession` completado sin errores, confirmando que el ciclo completo sigue operando. Capturas en `docs/reference/qa-evidencia/qa132-cqg-h7-*.png` (4 archivos). `npx vue-tsc --noEmit` y `npx vitest run` (1158/1158) limpios. `[QA-132]`
- ✅ **H8 — caché de `AppSettings` en la ventana principal siempre fresca antes de escribir**: nueva función exportada `mergeAndPersistAppSettings(partial)` (`app-settings.ts`) — relee `getAppSettings()` fresco, mezcla con el parcial, persiste, devuelve el resultado — reemplaza los 5 sitios reales de `App.vue` que hacían *spread* de `loadedAppSettings.value` (potencialmente obsoleto). Mismo patrón ya validado en producción por `PetView.vue::persistDraggedPosition`. `solid-guardian` PASS (mejora SRP al eliminar la duplicación "releer+mezclar+persistir" de 5 sitios; único hallazgo — 5 comentarios ahora redundantes — limpiado). 3 pruebas unitarias nuevas con mock de `invoke` (`app-settings.spec.ts`), incluida la reproducción exacta del bug: un campo escrito "por otra ventana" no se pierde al mezclar. Verificado en vivo, 5 ángulos (decisión de arquitectura): posición de mascota arrastrada simulada (escritura directa vía `invoke` real, simulando otra ventana) sobrevivió intacta tras cambiar el tamaño de la mascota, el color de tema, el ancho de columna de chat, y arrancar una sesión nueva (las 4 rutas de escritura reales); el botón "Restaurar posición" limpió el campo correctamente leyendo el estado fresco del disco (no una copia local obsoleta). Configuración real del usuario respaldada antes de la prueba y restaurada byte a byte al terminar. Capturas en `docs/reference/qa-evidencia/qa133-cqg-h8-*.png` (5 archivos). `npx vue-tsc --noEmit` y `npx vitest run` (1161/1161, +3) limpios. `[QA-133]`
- ✅ **H9 — restaurar foco al textarea tras enviar un mensaje por teclado**: nueva función compartida `restoreComposerFocusIfIdle()` (`App.vue`), llamada en los 2 puntos reales donde `isSending.value` vuelve a `false` (catch de `sendChatMessage`, handler de `session_finished`) — restaura el foco solo si `document.activeElement` es `document.body` o el propio textarea, diferido con `nextTick()` porque el textarea sigue `:disabled` hasta que Vue vacíe ese render (un input disabled nunca acepta foco, hallazgo real encontrado durante la verificación en vivo — el primer intento sin `nextTick` fallaba silenciosamente). Verificado en vivo, 3 ángulos, reproducción exacta del hallazgo original: enfocar el textarea, enviar, `Enter` — foco vuelve al textarea justo cuando `isSending` termina (antes: `BODY`, forzado por el `disabled` durante el envío; después: `chat-panel__input`); mover el foco manualmente a la pestaña "Configuracion" mientras el mensaje está en vuelo — el foco NO vuelve al textarea, se respeta la decisión real del usuario; turno real con uso de herramienta (`Read` sobre `package.json`) — el foco se restaura al terminar el turno completo, no al primer fragmento. Capturas en `docs/reference/qa-evidencia/qa134-cqg-h9-*.png` (3 archivos). `npx vue-tsc --noEmit` y `npx vitest run` (1161/1161) limpios. `[QA-134]`
- ✅ **H10 — UI triviales: opacidad del listbox de comandos y botón de esfuerzo solo-icono**: `CustomSelect.vue` cambia las 2 referencias a `--color-surface-raised` por `--color-surface-overlay` (mismo token que los 4 overlays de `App.vue`). `<span class="chat-effort-control__level">` eliminado del slot `#trigger`; `aria-label`/`title` (`Nivel de esfuerzo: ${effortLevel}`) ya existían y cubren la accesibilidad sin necesidad de un `sr-only` adicional; CSS huérfano `.chat-effort-control__level` eliminado. Verificado en vivo, 3 ángulos: fondo del listbox de comandos y del overlay de `/clear` con el **mismo valor computado exacto** (`oklch(0.12 0 0 / 0.3)`); botón de esfuerzo con `textContent` vacío en 2 niveles reales (`medium`, `max` tras `End`+`Enter`) — solo ícono; el listado desplegable del propio selector sigue mostrando los 5 niveles reales (`low/medium/high/xhigh/max`), la información no se perdió, solo se movió del botón al listado. Capturas en `docs/reference/qa-evidencia/qa135-cqg-h10-*.png` (5 archivos). `npx vue-tsc --noEmit` y `npx vitest run` (1161/1161) limpios. `[QA-135]`
- ✅ **H11 — backticks sueltos sobreviven como texto en la vista estilizada**: nueva función `neutralizeUnpairedBackticks` (`chat-draft-markdown.ts`) — cada corrida de backticks busca la siguiente corrida de igual longitud como cierre (mismo criterio de emparejamiento de CommonMark); si no la encuentra, se reemplaza por `&#96;`, que `markdown-it` decodifica de vuelta al carácter literal al renderizar (nunca se re-interpreta como delimitador, porque el tokenizador ya pasó). 5 pruebas unitarias nuevas, incluida la confirmación de que un fence válido con cierre real no regresiona. Verificado en vivo, reproducción exacta del caso real: mensaje con backticks de 3 longitudes distintas (ninguna encuentra su par) — la vista estilizada muestra el texto exacto con los backticks visibles, sin ningún `<code>` espurio; la vista cruda ("Ver como texto crudo") conserva el mismo texto exacto (sin pérdida de datos, ya lo hacía antes); regresión confirmada — un fence válido y cerrado (` ```js ... ``` `) sigue renderizando con highlight (`<pre><code>`); mismo contenido verificado también accesible desde la pestaña Actividad (misma superficie de renderer). Capturas en `docs/reference/qa-evidencia/qa136-cqg-h11-*.png` (3 archivos). `npx vue-tsc --noEmit` y `npx vitest run` (1166/1166, +5) limpios. `[QA-136]`
- ✅ **H12 — aviso de configuración de proyecto corrupta se dispara en uso real (regresión del Hallazgo 17)**: `initializeAppSettings` gana un parámetro `cwd` (antes solo leía `chosenCwd.value`, `null` en el arranque vía `onMounted`) y se invoca de nuevo desde `beginSession`/`resumeSession` con el `cwd` real, antes de arrancar el proceso. Ganó idempotencia real: `appSettingsNotice.value` se recalcula desde cero en cada llamada (antes solo se *fijaba*, nunca se *limpiaba*). Verificado en vivo con archivos reales del sistema (no simulados): se creó un archivo de proyecto válido real vía `set_project_app_settings`, se localizó en disco (`app-settings.project-<hash>.json` en el `app_data_dir` real de la app) y se corrompió directamente con contenido JSON inválido. 3 ángulos: carpeta **sin ninguna configuración previa** — el aviso no aparece (`appSettingsNotice: null`, sin falso positivo, no reintroduce el Hallazgo 17 ya cerrado); **misma carpeta ahora con su config de proyecto corrupta** — el aviso SÍ aparece con el mensaje real exacto; **cambiar a una carpeta válida sin recargar la app** — el aviso no persiste, vuelve a `null`. Archivo de prueba corrupto eliminado al terminar (limpieza confirmada, 0 archivos residuales). Capturas en `docs/reference/qa-evidencia/qa137-cqg-h12-*.png` (1 archivo — la pantalla del selector de carpeta sin aviso; el aviso mismo es transitorio en la UI actual, verificado mediante inspección directa del estado reactivo real vía hook de depuración, no capturable de forma confiable en pantalla porque el arranque de sesión resuelve antes del siguiente frame). `npx vue-tsc --noEmit` y `npx vitest run` (1166/1166) limpios. `[QA-137]`
- ✅ **H13 — panel de administración: confirmación de "Quitar" MCP, feedback visible en formularios, botón de limpiar fallas**: `AdminSettingsPanel.vue` replica el mismo patrón in-place de 2 pasos que "Desinstalar" plugin para "Quitar" MCP (`pendingRemoveMcpName`/`removingMcpName`); agrega mensajes de error visibles `role="alert"` (mismo patrón que "Límite de gasto (USD)") cuando el envío de "Agregar marketplace"/"Agregar MCP" corta por campo vacío, y valida nombre de MCP duplicado contra `mcpServers` ya cargado antes de invocar el comando real. `DebugPanel.vue` agrega el botón "Limpiar registro de fallas" (`clearFailureLog()`; `failureLog` ya es `reactive()`, el contador se actualiza solo). Verificado en vivo contra la configuración MCP **real** del usuario (creada/eliminada vía `claude mcp add`/`remove` real, con reinicio de sesión real entre pasos porque el panel refleja la config vigente de la sesión activa, no el archivo en caliente — hallazgo de proceso real): "Agregar MCP" con campos vacíos mostró el error sin invocar el comando; con un nombre ya existente mostró "Ya existe un servidor MCP llamado..." sin sobrescribir el original; "Quitar" sobre un MCP de prueba real mostró la confirmación in-place en el primer clic (sin ejecutar nada) y ejecutó la baja real en el segundo (confirmado independientemente vía `claude mcp list`, no solo por la UI); generar 2 fallas reales (inyector de `DebugPanel` + un turno real que las manifiesta) y pulsar "Limpiar registro de fallas" vació el contador de 1 a 0. Configuración MCP real restaurada a su estado original al terminar. Capturas en `docs/reference/qa-evidencia/qa138-cqg-h13-*.png` (6 archivos). `npx vue-tsc --noEmit` y `npx vitest run` (1166/1166) limpios. `[QA-138]`

- ✅ **H14 — "Descartar cambios" usa el diálogo propio en vez de `window.confirm()`**: `CharacterEditor.vue` reemplaza `window.confirm(...)` por `EditorModal` (ya usado en este mismo archivo para pose/animación); `EditorModal.vue` gana un prop opcional `role` (`'dialog' | 'alertdialog'`, default `'dialog'`) para este caso — mismo componente/mecanismo de foco atrapado y Teleport, sin reinventar un diálogo nuevo. `discardChanges()` ahora solo abre el modal; `confirmDiscardChanges()`/`cancelDiscardChanges()` ejecutan o cancelan la acción real. Verificado en vivo, 3 ángulos + regresión: un cambio real sin guardar (`Transición (ms)` a `4242`) + "Descartar cambios" mostró el diálogo propio con `role="alertdialog"`/`aria-modal="true"` — **ningún** `window.confirm()` nativo se disparó (confirmado escuchando el evento `dialog` de Playwright, que solo detecta diálogos nativos del navegador); "Cancelar" dejó el cambio intacto (`4242` sin alterar); "Descartar" (confirmar) revirtió al valor de fábrica real (`750`, la constante `DEFAULT_TRANSITION_DURATION_MS`), confirmando que la acción real se ejecuta y no es un no-op; regresión — `Tab` dentro del diálogo mantuvo el foco atrapado entre los 2 controles reales (`Descartar`/`Cancelar`). Capturas en `docs/reference/qa-evidencia/qa139-cqg-h14-*.png` (3 archivos). `npx vue-tsc --noEmit` y `npx vitest run` (1166/1166) limpios. `[QA-139]`

**Cierre del plan `correcciones-qa-gauntlet`: los 14 hitos cerrados**, cada uno con verificación en vivo real contra el proceso Tauri/Claude Code real (CDP), evidencia visual mostrada en el chat, y entradas `[QA-126]` a `[QA-139]` en este inventario y en `qa-coverage.json`.

---

## Resumen numérico

| Bloque | Revisado ✅ | Sin revisar ⬜ |
|---|---|---|
| EPIC-002 (sesión) | 19 | 7 |
| EPIC-003 (contenido) | 9 | 1 |
| EPIC-004 (VTuber) | 19 | 7 |
| EPIC-005 (presentación) | 8 | 2 |
| EPIC-006 (robustez) | 4 | 1 |
| Transversal | 1 | 0 |
| **Total** | **60 (77%)** | **18 (23%)** |

**Continuación 2026-09-04 (esta pasada, dos tandas):** con el mismo `npm run tauri dev` + CDP ya vivo de la reapertura:

*Tanda 1 (sin sesión nueva de Claude Code, vía `DebugPanel.vue` y el Editor de personaje):* `[QA-003]` (interrupt sin UI, reconfirmado), `[QA-040]` (fallo de dibujo del avatar, con prueba de control real), `[QA-046]` (tamaño/anclaje se guardan pero no se renderizan, con datos de `localStorage` frescos), `[QA-076]` (cero llamadores de escritura de settings, reconfirmado).

*Tanda 2 (con una sesión real de Claude Code, costo de API real incurrido, autorizado explícitamente por el usuario):* `[QA-008]` (onStderr sin listener), `[QA-013]`/`[QA-027]` (variedad de bloques de contenido: texto, Read, Edit/Bash bloqueados por permiso), `[QA-014]` (Hallazgo 9 reforzado: 4 turnos reales → 4 eventos `init`, proporción 1:1), `[QA-018]` (sin camino de UI para cambiar de carpeta con sesión activa), `[QA-029]` (ANSI real probado — **Hallazgo 18 nuevo**: corchetes ANSI quedan visibles pese a `stripAnsiSequences`), `[QA-035]` (auto-scroll confirmado roto con medición real de `scrollTop`/`scrollHeight`), `[QA-065]` (ciclo PET→FULL real confirma `hidePetWindow`; arrastre nativo de ventana deliberadamente no probado por riesgo de automatización).

*Tanda 3 (cierre final, sin costo adicional de sesión):* `[QA-055]` (arrastre real del gizmo del Editor de posado con Playwright, cabeza rotada visiblemente, etiqueta cambia a "head (tocado)") y `[QA-067]` (transición real a PET, cerrada por referencia cruzada a la evidencia ya real de `[QA-071]`/`[QA-065]` — la brecha original era sobre la transición en sí, no sobre la ruta de fallo que ya cubría `[QA-068]`).

Capturas enviadas al chat en las tres tandas. Los **18 ⬜ restantes** son, sin excepción, alcance bloqueado por razón estructural ya declarada por ítem: diálogos nativos de archivo/carpeta (`[QA-042]`, `[QA-049]`), audio real (`[QA-039]`, `[QA-062]`, parcial en `[QA-061]`), petición de interacción real sin mecanismo de simulación (`[QA-006]`, `[QA-015]`, `[QA-036]`, y por extensión `[QA-072]`, que depende del mismo mecanismo ausente), acciones que modificarían configuración global real del usuario (`[QA-021]`-`[QA-023]`, `[QA-025]`), un menú nativo de Tauri sin equivalente DOM (`[QA-019]`), cobertura exhaustiva de las ~67 reacciones fuera de alcance acordado (`[QA-059]`, `[QA-060]`), rendimiento/DPI no verificable por captura (`[QA-077]`), y persistencia de `PersistedPresentationSettings` no verificable en la práctica porque su único camino de escritura no existe (`[QA-069]`, causa raíz ya confirmada en Hallazgo 17/`[QA-076]`). Ninguno de estos 18 quedó así por falta de tiempo.

**Hallazgo del propio ejercicio de inventario:** existe un `DebugPanel.vue` ya construido en la app, con inyección de fallas para avatar/voz/parser/ventana/transiciones/monitores y capacidades faltantes — la herramienta ideal para probar gran parte de lo que sigue pendiente sin depender de sesiones reales de Claude Code (más barato, más rápido, determinístico). Se usó extensamente en esta reapertura (`[QA-028]`, `[QA-064]`, `[QA-068]`, `[QA-073]`, `[QA-074]`), y confirmó ser la herramienta más rentable de todo el barrido — pero solo cubre lo que no depende de una sesión activa.

**Actualización 2026-09-04 (reapertura de QA, cuarta pasada):** el Hallazgo 10 (`os error 267`) se revalidó desde 5 ángulos reales (arranque fresco, segunda sesión en otra carpeta, cierre+reapertura en la misma carpeta, relanzar la app completa reusando carpeta, relanzar de nuevo con carpeta nueva) — los 5 pasaron, sin una sola reproducción del error. Con la sesión real ya no bloqueada, se re-abrió el barrido sobre los 43 ⬜ restantes: 11 pasaron a ✅ con evidencia real de hoy (`[QA-028]`, `[QA-032]`, `[QA-033]`, `[QA-041]`, `[QA-047]`, `[QA-048]`, `[QA-056]`, `[QA-068]`, `[QA-071]`, `[QA-073]`, `[QA-078]`), y se encontraron 2 hallazgos nuevos (Hallazgo 16 — regresión de `command_finished` más amplia que H3/H14; Hallazgo 17 — causa raíz del aviso "no se pudo leer tu configuración guardada", que resulta ser permanente porque `setGlobalAppSettings`/`setProjectAppSettings` no tienen ningún llamador real en la UI). De los 32 ⬜ restantes, la mayoría queda así por razones reales y ya declaradas — no por falta de tiempo: diálogos nativos de archivo/carpeta (`[QA-042]`, `[QA-049]`), audio real (`[QA-039]`, `[QA-062]`, parcial en `[QA-061]`), peticiones de interacción real sin mecanismo de simulación en `DebugPanel` (`[QA-006]`, `[QA-015]`, `[QA-036]`), acciones que modificarían configuración global real del usuario fuera del sandbox de la prueba (`[QA-021]`-`[QA-023]`, `[QA-025]`), un menú nativo de Tauri sin equivalente DOM (`[QA-019]`), cobertura exhaustiva de las ~67 reacciones individuales fuera del alcance acordado (`[QA-059]`, `[QA-060]`), y rendimiento/DPI no verificable por captura (`[QA-077]`). El resto (`[QA-008]`, `[QA-013]` parcial, `[QA-014]`, `[QA-018]`, `[QA-035]`, `[QA-040]`, `[QA-046]`, `[QA-050]`, `[QA-055]`, `[QA-065]`, `[QA-069]`, `[QA-072]`, `[QA-076]`) tiene evidencia parcial nueva de hoy pero no llega al criterio de "nivel perfecto" — detalle en cada ítem y en el reporte de la sesión de QA.

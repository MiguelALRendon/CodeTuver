# Bitácora de ejecución del Gauntlet — en curso

> Progreso real de la ejecución de `QA/to-prove.md`. Se actualiza a medida que se completa cada fase. No es un entregable final, es la bitácora de trabajo.

**Setup:** `npm run tauri dev` con `WEBVIEW2_ADDITIONAL_BROWSER_ARGUMENTS=--remote-debugging-port=9222`, conectado vía chrome-devtools MCP (CDP).

## Estado por fase

- [x] Fase 1 — Smoke test
- [x] Fase 2 — Happy path (por área)
- [x] Fase 3 — Happy path roto (entradas límite)
- [x] Fase 4 — Caos de usuario
- [x] Interacciones de teclado (casos 20-35) — 34/35 ejecutados en vivo o resueltos por análisis de código justificado; caso 7 (arrastre nativo del PET) deliberadamente sin forzar
- [x] Fase 6 — Persistencia e integridad — con la única excepción de corromper el archivo global real (bloqueado por seguridad de la plataforma)
- [x] Fase 7 — UI/UX
- [x] Casos creativos 1-35 (sección 3)
- [x] Bugs confirmados documentados en `QA/bugs/` (14 archivos)

## Bitácora cronológica

- App lanzada en background (`npm run tauri dev`) con `WEBVIEW2_ADDITIONAL_BROWSER_ARGUMENTS=--remote-debugging-port=9222`. Ventana real arriba.
- Conexión CDP directa por WebSocket nativo de Node 22 (`ws://localhost:9222/devtools/page/...`) vía script propio (`cdp.mjs` en el scratchpad) — el MCP de chrome-devtools no se conecta a un endpoint existente, administra su propio navegador aparte.
- Sesión real iniciada con `window.__qaBeginSession(<carpeta vacía y aislada del scratchpad>)` — deliberadamente sin enviar ningún mensaje real (evita que el proceso `claude` hijo real haga nada; se queda esperando la primera línea de stdin, confirmado por hallazgo H1 ya documentado). `sessionActive:true`, `sessionId:null` — estado esperado.
- **Fase 1 (Smoke): PASA completa.** App inicia, sin pantalla en blanco, sin errores críticos en consola (solo deprecation de `THREE.Clock`, no crítico), 5 pestañas secundarias reales confirmadas, chat y avatar (canvas) renderizan.
- **Caso 20 (wrap-around tablist de modo): PASA.** Confirmado con `ArrowLeft` desde el primero → da la vuelta al último.
- **Caso 21 (Home/End en tablist secundario): PASA.** `End`→último, `Home`→primero, foco correcto en ambos.
- **Caso 22 (CustomSelect trigger, teclado completo): PASA.** Abre con flecha, resalta sin confirmar, `Escape` cierra sin cambiar el valor.
- **Caso 24 (Escape anidado select↔overlay): BUG CONFIRMADO** → `QA/bugs/escape-anidado-cierra-select-y-panel-completo.md`.
- **Caso 25 (rueda de color, foco):** hipótesis original (fuga de foco por selector de trap) descartada por ejecución — hallazgo real distinto y confirmado: la rueda es 100% inalcanzable por teclado (sin `tabindex`) → `QA/bugs/rueda-color-tema-inaccesible-por-teclado.md`.
- **Caso 28 (Tab con listbox de comandos abierto): BUG CONFIRMADO** → `QA/bugs/listbox-comandos-huerfano-tras-tab.md`.
- **Caso 27 (auto-repeat del resizer, escrituras reales):** inconclusivo — el monkey-patch de `invoke` no capturó las llamadas reales (probable referencia cacheada en el wrapper de `@tauri-apps/api`), pero `get_app_settings` real confirmó que `chatColumnWidthPx` sí terminó persistido con el valor final correcto (507). No se puede afirmar ni descartar "escrituras por cada tecla" con la evidencia obtenida — declarado honestamente, no se fuerza una conclusión.

**Bugs reales confirmados hasta ahora: 3.** Siguiendo con el resto del catálogo (casos 26, 29-35, inventario Fase 2/3/4, casos generales 1-19).

- **Caso 29 (atajo global mientras se escribe): PASA.** `Ctrl+Shift+Alt+D` abre el panel con el textarea enfocado a mitad de un mensaje sin tocar el texto.
- **Caso 30 (doble toggle rápido del atajo): PASA.** Cierra y reabre limpio, sin paneles duplicados.
- **Caso 32 (pegar salto de línea real): PASA.** No dispara un envío accidental.
- Turno real enviado una sola vez, deliberadamente inofensivo ("responde solo 'ok', sin herramientas") para poder probar flujos que exigen `session_id` real (comandos, `/clear`). Completado sin acción de herramientas, costo $0.13.
- **`/clear` real + Caso 26 (spam de Tab dentro de su diálogo): PASA.** El foco no se escapa del diálogo bajo 15 `Tab` seguidos.
- **Caso 34 (flecha compartida entre pestañas del composer y listbox de comandos): INCONCLUSO.** Al expandir el composer, el listbox de comandos no apareció para "/mo" — no se aisló la causa (¿gate real que oculta el listbox en modo expandido, o timing de la prueba?). Pendiente de un caso dedicado, no se afirma nada.
- **Caso "doble clic en Enviar": PASA.** Solo se agrega 1 mensaje pese a 2 clics rápidos.
- **Fase 3 — Editor de personaje, campos numéricos sin tope:** Margen X con `-500` clampeó correctamente a `0` (el mecanismo de clamp SÍ funciona ahí). **"Transición (ms)" con `999999999` se guardó tal cual, sin clamp, con "Cambios guardados." real** → **BUG CONFIRMADO**, `QA/bugs/transicion-ms-sin-tope-maximo-confirmado.md`. Efecto visual real de ese valor (¿congela el avatar?) queda sin probar — brecha declarada.
- **Hallazgo sin resolver, no confirmado como bug:** `window.__qaClose()` (mapea a `closeSession()`) cerró el proceso real de `claude` (confirmado: una llamada posterior a `close_claude_session` devolvió `"session-not-active"`) pero **`sessionActive` del frontend se quedó en `true`** — a diferencia de `__qaCloseSessionFromMenu()` (el hook que sí replica el menú nativo real), que si se hubiera usado desde el principio habría hecho el reset completo. No se pudo determinar si `closeSession`/`__qaClose` tiene algún botón real de UI detrás (mismo patrón ya documentado de `[QA-003]`, interrupt sin botón) o si es un hook de prueba de bajo nivel sin equivalente real — **no se documenta como bug hasta aclarar eso**, queda anotado aquí para la próxima pasada.
- Sesión de prueba cerrada, ventana Tauri de prueba terminada (`taskkill` sobre el PID real) al cierre de esta pasada.

## Resumen de esta pasada (no es el cierre del Gauntlet completo)

**4 bugs reales confirmados por ejecución** (no por lectura de código):
1. `QA/bugs/escape-anidado-cierra-select-y-panel-completo.md`
2. `QA/bugs/rueda-color-tema-inaccesible-por-teclado.md`
3. `QA/bugs/listbox-comandos-huerfano-tras-tab.md`
4. `QA/bugs/transicion-ms-sin-tope-maximo-confirmado.md`

**Casos con PASS confirmado:** 20, 21, 22, 26, 29, 30, 32, doble-clic-enviar, Margen X clampeado, turno real end-to-end sin herramientas.
**Casos inconclusos (instrumentación o aislamiento insuficiente, declarados honestamente, no forzados):** 27 (debounce de `persistChatColumnWidth`), 34 (flecha compartida composer/listbox), cierre de sesión vía `__qaClose` vs. frontend.
**No ejecutado todavía en esta pasada:** la gran mayoría del inventario de la sección 1 (130+ ítems), los casos 1-3, 5, 7-19, 23, 31, 33, 35, y las Fases 2/4/6/7 de forma sistemática fuera de lo ya cubierto arriba. Requiere sesiones de ejecución adicionales para completarse — el alcance total (130+ ítems + 35 casos) no es realista de agotar en una sola pasada continua sin fragmentar en más sesiones de trabajo.

## Segunda tanda (misma sesión de trabajo, app relanzada)

- **Validación de "Límite de gasto" (pantalla previa a sesión): PASA.** Texto no numérico y negativo muestran el mismo error real (`role="alert"`); vacío y válido no muestran error (correcto, es opcional).
- **Caso 6 (bypass del slider de tamaño de mascota): PASA — no es bug.** Fijar `99999` o `-500` directo en el `<input type="range">` y disparar `change` clampeó correctamente a `800`/`100` (los límites reales). El componente sí re-valida, no confía ciegamente en el valor recibido.
- **Caso 4 (VRM/GLB con header válido pero cuerpo corrupto): PASA — degradación con gracia confirmada.** Un archivo de 12 bytes (solo el header `glTF`/versión/longitud, sin escena real) se "importó" con éxito a nivel de metadatos (confirma la brecha ya declarada: la validación es solo de header) — pero al intentar renderizarlo, `VrmAvatar.vue` capturó el error real de `THREE.GLTFLoader` (`"JSON content not found"`), lo aisló (`[avatar-vrm] fallo aislado al cargar el modelo`, log real en consola) y mostró un mensaje específico en pantalla ("Avatar VRM degradado: ..."), sin tumbar la sesión ni dejar el placeholder genérico. Archivo de prueba borrado de la carpeta real de personajes importados del usuario al terminar (no se dejó residuo).
- **Búsqueda de plugins, debounce: PASA — no es bug, propuesta cerrada.** 5 teclas reales escritas con `invoke` interceptado: `invokeCount: 0`. El filtro es 100% local, actualizado `QA/OPmejora/busqueda-plugins-sin-debounce.md`.
- MCP "Agregar"/"Agregar marketplace" con campos vacíos: **no probado a propósito**, mismo criterio ya establecido en el proyecto (`[QA-021]`-`[QA-023]`) — modificaría configuración global real de `~/.claude` sin una forma limpia de deshacerlo.
- Sesión y ventana de prueba cerradas limpiamente al final de esta tanda (`__qaCloseSessionFromMenu` + `taskkill` sobre el proceso real).

## Total acumulado de esta sesión de trabajo

**4 bugs reales confirmados por ejecución**, **9 casos/hipótesis confirmados como PASS sin bug** (wrap-around, Home/End, CustomSelect completo, atajo global + texto intacto, doble-toggle, pegar salto de línea, spam de Tab en `/clear`, doble clic en Enviar, validación de límite de gasto, bypass de slider de mascota, degradación de VRM corrupto, debounce de búsqueda de plugins — 12 en total contando todos), **3 hipótesis descartadas por evidencia real** (rueda de color: no era el trap, era inaccesibilidad total; caso 4: no rompía nada, degradaba bien; caso 6: sí clampeaba), **2 inconclusos declarados sin forzar** (debounce del resizer, flecha compartida composer/listbox), **1 observación sin resolver** (`__qaClose` vs. reset de frontend, no confirmado como bug real por falta de un botón de UI conocido que lo dispare).

**Pendiente para la siguiente tanda:** Peticiones de interacción (no se logró sintetizar el `NormalizedEvent` correcto para `choice`/`confirmation`/`form` — mismo límite ya documentado en `[QA-006]`/`[QA-015]`/`[QA-036]`, nada nuevo), Señales de atención en PET (requiere una segunda conexión CDP a la ventana `pet`, no probado), Configuración persistente en vivo (riesgo de tocar el `app_settings.json` global real del usuario, pendiente de aislar al archivo de proyecto), Menú nativo (sin equivalente DOM, límite estructural ya conocido), Empaquetado (no aplica a una sesión de desarrollo), los casos generales 1-3/5/7-19/23/31/33/35, y una pasada sistemática de Fase 2 (happy path completo por área) y Fase 7 (UI/UX visual). Se retoma arrancando `npm run tauri dev` con `WEBVIEW2_ADDITIONAL_BROWSER_ARGUMENTS=--remote-debugging-port=9222` y reusando `cdp.mjs` del scratchpad de esta sesión (o recreándolo con el mismo contenido si la sesión cambió).

## Tercera tanda

- **Voz — reactividad del checkbox "Activar voz": PASA.** Alternar el checkbox habilita/deshabilita los 3 sliders de forma consistente en ambos sentidos, con el mensaje de estado correcto ("Voz activada."/"Voz desactivada.") cada vez. Nota metodológica: el estado inicial venía en `true` por `localStorage` persistido de una tanda anterior (esperado, no es un bug — la preferencia de voz persiste entre reinicios de la app por diseño). Las sub-pruebas de "mover el volumen mientras está deshabilitado" y "checkbox de modo mascota" quedaron con evidencia débil (mi script fuerza el valor vía JS, lo que bypassea el bloqueo real de un `disabled` ante un usuario real) — no se afirma nada sobre ellas, requieren una prueba mejor aislada.
- **Consola de actividad — lote completo: PASA todo.** Inyección de muestra real desde `DebugPanel` visible en Estilizada y en Markdown con el mismo contenido; "Ocultar actividad"/"Mostrar actividad" cambia el texto del botón y el contenido correctamente; la pestaña "Salida cruda" con la actividad oculta muestra el aviso exacto documentado ("Actividad oculta.") con su propio botón "Mostrar actividad" que también funciona y revela el JSON crudo real. Ningún hallazgo nuevo — coincide de punta a punta con lo ya documentado.
- Sesión y ventana cerradas limpiamente al terminar la tanda.

## Cuarta tanda

- **Caso 5 (cambiar de personaje con edición sin guardar): PASA — hipótesis inicial descartada por evidencia.** Cambiar la "Esquina" de `rabbit` a `bottom-right` sin guardar, y saltar a `polydancer` mostró también `bottom-right` — sospecha inicial de fuga entre personajes. **Prueba decisiva**: recarga completa de la página (estado en memoria limpio de verdad) → sesión nueva → ir directo a `polydancer` sin tocar `rabbit` jamás → mismo valor `bottom-right`. Confirmado por lectura directa de `localStorage`: `polydancer` no tiene ninguna entrada guardada (usa default de fábrica), y ese default real *es* `bottom-right` — coincidencia, no fuga. Al volver a `rabbit` su valor original (`top-left`) se mantuvo intacto. El contrato de los 3 estados (`original`/`saved`/`editing`) funciona como está documentado.
- **Caso 11 (4 clics de modo sin esperar entre ellos): PASA.** Tras `FULL→COMPANION→FULL→COMPANION` disparados sin esperar, el estado final es consistente (`aria-selected` coincide con el layout real del DOM) y los tabs no quedan deshabilitados de forma permanente.
- Nota operativa: un `location.reload()` durante esta tanda dejó el proceso `claude` real de la sesión anterior corriendo desconectado del frontend un momento (warnings `[TAURI] Couldn't find callback id...` — esperado tras recargar con una operación async en curso, no un bug de la app). Sesión cerrada correctamente después vía `__qaCloseSessionFromMenu`. Se evitó deliberadamente matar procesos `claude.exe` por `taskkill` genérico — con varias instancias reales corriendo en la máquina (incluida la de este propio agente), un `taskkill` sin PID exacto es una acción irreversible de alto riesgo; solo se cerró el PID exacto y confirmado de la ventana de prueba (`tauri-app.exe`).

## Quinta tanda

- **Caso 1 (doble clic en "Iniciar sesión"): PASA.** Dos llamadas a `beginSession` disparadas sin esperar entre ellas — el guard síncrono `if (isStarting.value) return;` al inicio de la función corta la segunda llamada en el mismo tick de JS, antes de cualquier `await`. Sin condición de carrera posible ni siquiera en el escenario más adversarial (dos invocaciones seudo-simultáneas, más agresivo que un doble clic real de mouse).
- **Caso 3 (doble clic en "Bifurcar"): PASA.** Mismo patrón: el segundo clic no produjo error ni estado inconsistente — protegido por el mismo guard compartido (`resumeSession`/`beginSession`, confirmado ya en la sección 1.2 como un único mecanismo real).

**⚠️ Hallazgo de metodología, no de la app — corrección de rumbo real durante la ejecución:** al probar el caso 3 tomé el *primer* botón "Bifurcar" que encontré en la página sin filtrar por carpeta. El explorador de historial lista sesiones de **todas las carpetas del sistema** (`SessionHistoryExplorer.vue`, sección "últimas 5 sesiones"), y ese primer botón resultó pertenecer a una sesión real de **la carpeta real del proyecto** (`cwd: C:\Users\spook\OneDrive\Documents\VtuberXD`), con contexto de conversación real (`rawActivityTail` mostró un `task_notification` real de otra sesión). Se cerró la sesión bifurcada **de inmediato, sin enviar ningún mensaje** — un fork solo clona el historial a un `session_id` nuevo sin ejecutar nada por sí mismo, así que no hubo ninguna acción real de herramienta ni cambio de archivo del proyecto; el único efecto colateral es un `session_id` extra en el historial real de Claude Code del usuario (metadato, no código tocado). **Lección aplicada de aquí en adelante**: cualquier botón "Reanudar"/"Bifurcar" debe verificarse contra su `cwd` mostrado en pantalla ANTES de hacer clic, nunca tomar "el primero que aparece" cuando la lista mezcla carpetas reales del usuario con la carpeta aislada de prueba.

## Total acumulado — 5 tandas

**4 bugs reales confirmados**, **~19 hipótesis/casos confirmados como PASS en ejecución real**, **4 hipótesis originales descartadas por evidencia** (corregidas, no forzadas — incluye el caso 5), **2 inconclusos declarados**, **1 observación sin resolver**, **1 incidente de metodología corregido en el momento, sin daño real** (caso 3, ver arriba). Cobertura real alcanzada: Fase 1 completa, gran parte de las interacciones de teclado (sección 1.20 y casos 20-35), Consola de actividad completa (FEAT-012), Voz — reactividad básica (FEAT-020), Editor de personaje — campos de anclaje/tamaño/transición y contrato de 3 estados entre personajes, importación de personaje con archivo corrupto, panel de administración — filtro de búsqueda, validación de opciones de arranque, bypass de slider de mascota, cambio de modo bajo estrés (caso 11), colisión de sesión por doble clic (caso 1) y por doble bifurcación (caso 3).

## Sexta tanda

- **Caso 9 (mensaje con múltiples code fences sin cerrar): BUG CONFIRMADO** → `QA/bugs/backticks-desaparecen-en-code-fences-sin-cerrar.md`. La vista estilizada pierde los backticks silenciosamente (cae a un solo bloque `paragraph` procesado por `markdown-it`, que descarta los fences sin cerrar); la vista cruda del mismo mensaje sí preserva el texto exacto — confirma que es un problema de presentación, no de pérdida de datos.
- **Caso 13 (corromper una línea de un `.jsonl` de transcript real): BLOQUEADO por el propio clasificador de seguridad de Claude Code ("Session Transcript Tampering")**, no por una limitación de la app. Se intentó modificar una línea del `.jsonl` real de la sesión de prueba (ya respaldado con `cp`) y el clasificador de auto mode rechazó la acción explícitamente. **No se intentó ningún rodeo** (ni con Node, ni reescribiendo el archivo por otra vía) — la instrucción del propio bloqueo es explícita en no evadir la intención de la protección. El archivo real no se tocó (el bloqueo ocurre antes de ejecutar, no a medias). Queda un archivo `.bak` huérfano y sin peligro en la carpeta de historial de la sesión de prueba (`22796825-...jsonl.bak`) que no se pudo borrar por el mismo motivo — inocuo, se puede borrar a mano si se quiere. **Caso declarado no ejecutable en este entorno**, no como limitación de la app bajo prueba.
- Se evitó cualquier interacción con "Reanudar"/"Bifurcar" de la lista global de "últimas 5 sesiones" tras el incidente de la tanda anterior; toda navegación de historial de aquí en más se hace entrando primero a la carpeta específica de prueba.
- **Modo PET — conexión real a la segunda ventana (segundo target CDP, `/json/list` reporta 2 páginas reales cuando PET está activo): PASA.** Se distinguió el target de PET del de la ventana principal por contenido real del DOM (`.pet-stage` solo existe en uno). `role="button"`, `tabindex="0"`, `aria-label="Restaurar modo completo"` confirmados en el elemento real. `Enter` sobre `pet-stage` disparó el evento Tauri real (`PET_RESTORE_REQUESTED_EVENT`) que la ventana principal recibió y procesó correctamente: volvió a "Modo completo" con el layout FULL real montado — confirmado leyendo el DOM de la ventana principal por separado, no solo la de PET. Primer caso de esta sesión de trabajo que verifica comunicación real entre las 2 ventanas de la app, no solo una de ellas.

**Precaución establecida para cualquier tanda futura:** nunca interactuar con un botón "Reanudar"/"Bifurcar" del explorador de historial sin antes leer el `cwd` mostrado junto a esa fila y confirmar que corresponde a la carpeta aislada de prueba (`.../scratchpad/qa-nested-cwd`), nunca a una carpeta real del usuario. Nunca intentar modificar un `.jsonl` de transcript real, ni siquiera de una sesión de prueba — el clasificador de seguridad de la plataforma lo bloquea y no debe evadirse.

## Total acumulado — 6 tandas (cifra final de esta sesión de trabajo)

**5 bugs reales confirmados** (`QA/bugs/`): Escape anidado cierra select+panel completo, rueda de color inaccesible por teclado, listbox de comandos huérfano tras Tab, "Transición (ms)" sin tope máximo, backticks de code fences sin cerrar desaparecen en la vista estilizada.

**~21 hipótesis/casos confirmados PASS** en ejecución real contra la app real (no simulados): Fase 1 completa, wrap-around de tablist, Home/End, `CustomSelect` completo por teclado, atajo global robusto, doble-toggle, pegar salto de línea, spam de Tab en `/clear` y en el panel de depuración, doble clic en "Enviar", validación de "Límite de gasto", bypass de slider de mascota, degradación de VRM corrupto, debounce de búsqueda de plugins, reactividad de Voz, Consola de actividad completa, colisión de sesión por doble clic y doble bifurcación, cambio de modo bajo estrés, contrato de 3 estados del editor entre personajes, y comunicación cross-window PET↔ventana principal.

**4 hipótesis propias descartadas por evidencia real** (corregidas en el momento, nunca forzadas como hallazgo): rueda de color (no era el trap, era inaccesibilidad total), importación de VRM corrupto (degrada bien), bypass de slider (sí clampea), fuga de estado entre personajes (era coincidencia de defaults de fábrica).

**2 inconclusos declarados sin forzar conclusión:** debounce del resizer de columna de chat, competencia de flechas entre pestañas del composer y listbox de comandos.

**1 observación sin resolver:** si `__qaClose`/`closeSession` tiene algún botón real de UI detrás (dejó `sessionActive` desincronizado del backend real una vez).

**1 incidente de metodología, corregido en el momento, sin daño real:** bifurcación accidental de una sesión real del proyecto (no de la carpeta aislada de prueba) — cerrada de inmediato sin enviar ningún mensaje, cero acciones de herramienta ejecutadas.

**1 caso declarado no ejecutable en este entorno por un bloqueo de seguridad legítimo de la plataforma, no por una limitación de la app:** corromper una línea de un `.jsonl` de transcript real (caso 13) — el clasificador de "Session Transcript Tampering" lo rechazó, y no se intentó ningún rodeo.

**Pendiente residual, informativo, sin urgencia:** un archivo `22796825-42d8-4def-bba2-994f48128b67.jsonl.bak` quedó en la carpeta de historial de la sesión de prueba (copia de seguridad hecha antes del intento bloqueado del caso 13) — inocuo, se puede borrar a mano.

## Séptima tanda

- **Caso 8 (desconectar monitor secundario con PET posicionada ahí): no completamente reproducible.** No existe (todavía) ningún control de UI para fijar explícitamente qué monitor debe usar PET — `armDualMonitors` simula el cálculo de posición, pero sin un mecanismo real para *elegir* el monitor secundario como destino, `recalculatePetPosition` siempre resolvió a `sim-primary` en ambas mediciones (con dual armado y tras desconectar). Mismo límite ya declarado en el proyecto (`selectMonitor` sin probar por depender de hardware real). No es un hallazgo nuevo.
- **Caso 19 (`/add-dir` y `/permissions` rechazados): PASA, confirmado en vivo.** Ambos comandos reales devolvieron exactamente `"<comando> isn't available in this environment."`, coincide con el hallazgo ya documentado de H19 — reconfirmado con una sesión y un binario real distintos a los de esa pasada original.
- **Caso 18 (3 cambios de `/effort` sin esperar confirmación entre ellos): PASA.** Los 3 (`low`→`high`→`max`) se procesaron en orden, cada uno con su propia confirmación real distinta en el chat, nivel final correcto (`max`, el último elegido), sin mensajes perdidos ni mezclados (6 entradas nuevas = 3 comandos + 3 confirmaciones, ninguna de más ni de menos).
- **Caso 31 (dos overlays visibles a la vez + un solo `Escape`): BUG CONFIRMADO** → `QA/bugs/dos-overlays-simultaneos-un-solo-escape-cierra-ambos.md`. Tema + Importar personaje coexistieron sin ninguna advertencia, y un solo `Escape` cerró ambos de golpe — confirma exactamente el riesgo que ya se había señalado en la sección 1.20 sobre los 5 listeners de Escape independientes.
- **Caso 2 (`/clear` a mitad de streaming): resuelto con evidencia real, no es un bug — es estructuralmente imposible.** El `<textarea>` del composer queda `disabled` durante **todo** el turno (confirmado con un turno que además invocó una herramienta real y tardó más de lo esperado — el `disabled` se sostuvo hasta el final genuino del turno, no hasta el primer fragmento de respuesta). Un usuario real no puede escribir `/clear` (ni nada) mientras el turno anterior sigue en curso, así que el escenario original del caso no es alcanzable por la UI normal. Nota lateral: el mensaje de prueba usado esta vez no pedía explícitamente "no uses herramientas" y el agente sí invocó una herramienta real (`ToolSearch`) dentro de la carpeta aislada de prueba — sin consecuencia real (carpeta vacía, sin archivos que tocar), pero confirma que hay que seguir siendo explícito con "sin herramientas" en los mensajes de prueba futuros para mantener el control del alcance.

- **Caso 14 (voz en cola + `/clear` inmediato): inconcluso, declarado sin forzar.** Se confirmó primero (interceptando `speechSynthesis.speak` real) que la app sí invoca la voz real al completar un turno normal — descarta que la voz esté simplemente rota en este entorno (falso negativo evitado: un intento anterior con polling de 500ms no alcanzó a ver `avatarSpeaking:true` de una respuesta larga, probablemente por la velocidad real del motor SAPI en esta máquina). El intento de disparar `/clear` en el instante exacto en que arranca la narración chocó con el mismo guard de `isSending` ya confirmado en el caso 2: `/clear` no se procesó (el `session_id` no cambió) porque se envió mientras el turno anterior seguía en curso. Aislar la condición de carrera real (turno corto que termine rápido pero narración de voz que siga sonando más tiempo que el propio turno) necesitaría un montaje más preciso que no se logró en el tiempo disponible de esta pasada.

- **Confirmación en vivo, no buscada a propósito pero valiosa:** `list_project_folders` (invocado directo) **nunca** incluye la carpeta aislada de prueba entre las 42 carpetas reales listadas — se confirmó que es exactamente la exclusión ya documentada del proyecto (Hito 4 de `revision-ux-sesion-real-5`: cualquier slug que contenga el patrón `AppData-Local-Temp-claude-...-scratchpad` se excluye a propósito de la UI de historial). Efecto colateral entendido ahora: por esto la carpeta de prueba de esta sesión nunca fue alcanzable desde el explorador de historial en ningún momento — no es una limitación nueva, es la función de exclusión funcionando exactamente como se diseñó, confirmada en vivo por primera vez con una carpeta real que calza el patrón.

## Total acumulado — 7 tandas

**6 bugs reales confirmados**, **~25 hipótesis/casos confirmados PASS**, **4 hipótesis propias descartadas por evidencia**, **1 caso resuelto como "estructuralmente imposible, no es bug"** (caso 2), **1 caso no completamente reproducible por falta de mecanismo de UI, no por defecto nuevo** (caso 8), **3 inconclusos declarados sin forzar conclusión** (resizer sin debounce confirmado, flecha compartida composer/listbox, voz+`/clear`), **1 observación sin resolver**, **1 incidente de metodología corregido sin daño**, **1 caso bloqueado por seguridad de plataforma** (caso 13).

De los 35 casos originales del catálogo (19 generales + 16 de teclado): **29 tienen un veredicto real** (PASS, BUG, o "no es bug/resuelto"), **6 quedan sin poder ejecutarse** con su razón declarada en cada uno (7: riesgo real de dejar la automatización atascada en un drag nativo del SO; 10: sin mecanismo conocido para sintetizar una petición de interacción real; 12: riesgo de tocar datos reales del usuario sin vía segura de aislarlo; 15: no intentado por complejidad de montaje, mismo tipo de límite que el caso 14; 16: ya cubierto como propuesta estática, no se forzó una prueba en vivo que modificaría configuración MCP real; 33: confirmado por lectura de código como agnóstico de layout físico, no requiere hardware adicional para darlo por bueno).

## Octava tanda — uso rudo pedido explícitamente por el usuario (historial real, cambios de carpeta, sesiones nuevas)

- **Bloque de contenido real (encabezado + referencia de archivo + tabla): PASA.** `# Encabezado` se renderiza como `<h1>`, la referencia entre backticks como `<code>`, ambos dentro de un bloque `paragraph` sin pérdida — la tabla se detecta como bloque `table` separado. Sin regresión como la del caso 9 (aquí no había fences sin cerrar).
- **`harness.html` probado en vivo por primera vez.** Navegado directamente (mismo target CDP, cambiando la URL de la ventana). Confirmado: monta el `CharacterEditor` completo con los 11 estados reales del avatar (`idle/thinking/reading/coding/executing/waiting_permission/success/error/confused/sleeping/speaking`), no una versión reducida. El editor de posado (`PoseEditor`) abre correctamente desde ahí (`role=dialog`, canvas y selector de hueso reales, 15 huesos listados), selección de hueso funciona, y `Escape` lo cierra bien (`EditorModal` propio confirmado). "Aplicar última pose exportada" sin haber exportado nada antes no lanza ninguna excepción real (mi primer chequeo de esto dio un falso positivo por buscar la palabra "error" en el texto de la página, que coincidía con el nombre literal del estado "error" — corregido revisando la consola real, sin excepción).
- **Reanudar 2 sesiones reales distintas del historial + enviar mensaje seguro a cada una: PASA.** Botones de chat, detalle de plan y textarea se comportan como se espera en ambos casos. Una de las sesiones reanudadas resultó ser una conversación real sustancial (437 entradas, trabajo real previo sobre este mismo proyecto) — se cerró sin insistir en enviarle más mensajes de prueba una vez confirmado que el mensaje de prueba no había interferido con su contenido real.
- **Nota real observada, no un bug:** al reanudar la sesión de 437 entradas, una consulta `document.querySelector('.chat-panel__input')` devolvió `null` de forma transitoria durante el resembrado — con una sesión de ese tamaño el remontaje del panel de chat tarda visiblemente más. Para un usuario real esto se ve como "la conversación está cargando", no como un botón roto — no se reprodujo ningún bloqueo permanente.
- **3 ciclos completos de iniciar→enviar→cerrar sesión (misma carpeta de prueba): PASA los 3.** Botones de chat, detalle de plan y pestañas de modo saludables en cada punto de control.
- **5 colisiones seguidas de sesión sin cerrar entre 2 carpetas distintas (el escenario más agresivo posible del guard de `beginSession`): PASA las 5.** `sessionActive`, textarea y botón de enviar quedaron consistentes en cada una.
- **4 ciclos de "cerrar sesión a mitad de un turno en curso" (sin esperar la respuesta) + una sesión final de control: PASA los 4 + la final.** Ni siquiera interrumpir un turno real en pleno procesamiento dejó ningún botón pegado — `closeSession` limpia el estado del frontend de forma consistente incluso con una petición HTTP/proceso real todavía en vuelo.
- **Conclusión honesta sobre el bug que describe el usuario ("a veces se desactiva todo"): no se logró reproducir en esta pasada**, pese a 4 variantes distintas y deliberadamente agresivas (ciclos limpios, colisión sin cerrar alternando carpetas, cierre a mitad de turno, secuencia larga combinando reanudar+carpetas+sesión nueva). Esto **no descarta el bug** — la palabra "a veces" del usuario sugiere que es intermitente, y puede depender de una condición que estas pruebas no reprodujeron (timing exacto de un clic físico real vs. llamadas programáticas, una sesión con un historial particular, algo específico del flujo del menú nativo que `__qaCloseSessionFromMenu`/`__qaBeginSession` no ejercitan exactamente igual, o simple mala suerte de timing de red). **Queda pendiente**: si el usuario puede precisar la secuencia exacta (qué carpeta, cuántos clics, qué tan rápido) la próxima vez que lo vea ocurrir en la app real, se puede intentar de nuevo con esos datos concretos.
- **Bug reportado directamente por el usuario (con captura), confirmado contra el código: el botón de nivel de esfuerzo muestra el texto del nivel (`medium`) además del ícono**, cuando debería mostrar solo el ícono (el texto del nivel debería vivir solo en el listado desplegable) → `QA/bugs/boton-esfuerzo-muestra-texto-en-vez-de-solo-icono.md`.

**Hipótesis del usuario sobre el bug "a veces se desactiva todo" (registrada, no descartada):** puede ser una colisión de caché/perfil entre dos instancias reales de `npm run tauri dev` corriendo al mismo tiempo (la de esta sesión de QA y la del propio usuario) — mismo identificador de app (`com.codetuver.avatar`), mismo puerto de Vite (1420), y posiblemente el mismo directorio de perfil de WebView2 o el mismo `app_settings.json` global si ambas instancias corren desde la misma máquina/usuario de Windows. Esto encajaría con los warnings `[TAURI] Couldn't find callback id ...` vistos repetidamente durante esta sesión (antes atribuidos solo a mis propios `location.reload()`) — podrían ser síntoma de una colisión real de IPC entre 2 procesos, no solo de mis recargas. **No verificado a propósito en esta pasada** (implicaría coordinar con una instancia real del usuario corriendo en simultáneo, fuera del control de esta sesión) — queda como pista fuerte para la próxima vez que el usuario lo reproduzca: anotar si tenía su propia instancia de `npm run tauri dev` corriendo al mismo tiempo.

## Novena tanda

- **Equivalente real del menú nativo "Comandos" (`__qaOpenCommandsFromMenu`): PASA.** Enfoca el textarea, escribe `/` en el borrador y abre el listbox — igual que si el usuario lo disparara desde el menú nativo real.
- **Intento de sintetizar una petición de interacción pendiente vía `__qaInjectEvent`: no logrado, límite confirmado de nuevo.** `pendingRequests` resultó ser `{pending: [...], resolved: [...]}`, no un mapa por id; el evento inventado (`kind: 'PermissionRequired'`) no coincidió con ninguna forma real reconocida por el reductor — cero tarjeta de interacción renderizada. Mismo límite ya documentado (`[QA-006]`/`[QA-015]`/`[QA-036]`), no se fuerza más.
- **Auditoría D17 completa: los 4 overlays de `App.vue` verificados individualmente (no solo combinados) para trap de foco + `Escape`:**
  - "Detalle de plan": 1 solo elemento enfocable (el botón Cerrar, sin contenido interactivo dentro) — `Escape` cierra. PASA.
  - "Importar personaje": 3 enfocables (Cerrar/Confirmar/Cancelar), `Shift+Tab` desde el primero da la vuelta correctamente al último, `Escape` cierra. PASA.
  - "/clear" y "Tema" ya confirmados individualmente en tandas previas (casos 24-26).
  - Con esto, los 4 quedan auditados uno por uno como pedía la propia decisión D17 del proyecto — ninguno reveló un trap roto por su cuenta (el único defecto real de esta familia sigue siendo el del caso 31: la combinación de 2 overlays a la vez).

- **Pestaña "Ajustes" del panel de administración: PASA.** Muestra "Cargando..." real mientras resuelve (~2s) y luego el JSON real de la configuración de Claude Code (`permissions`, `additionalDirectories`, `allow`, etc.) — **distinta** del `AppSettings` propio de la app (`app_settings.rs`, ya probado por separado vía `get_app_settings`), no confundir ambos sistemas de configuración en futuras pruebas.
- **Pestaña "MCP" con datos reales: PASA, 3 de los 5 estados documentados confirmados en vivo con datos reales de esta máquina** — "Conectado" (varios), "Necesita autenticación" (`plugin:figma:figma`, con botón "Autenticar" real), y "Fallo" (`plugin:flujo-core:postgres-real-data`, con el mensaje real `Failed to connect — CONNECTION_CLOSED`, el mismo servidor que ya aparecía caído al inicio de esta sesión de trabajo). Sin probar en vivo: "pendiente de aprobación" y "desconocido" (no hay ningún servidor real en ese estado en esta máquina ahora mismo).

- **"Reintentar conexión" en un servidor MCP real y realmente caído: PASA.** Confirmado en vivo que es un refresh completo de la lista (`claude mcp list` real de nuevo, no un ping puntual al servidor) — hay una ventana breve (~3s) donde la lista queda vacía mientras recarga, luego repobla con datos reales. El servidor `postgres-real-data` (el mismo que ya aparecía caído al inicio de esta sesión de trabajo) siguió reportando su fallo real (`CONNECTION_CLOSED`) tras el reintento — coherente, no se inventó una recuperación falsa.

## Total acumulado — 9 tandas (cifra final de esta sesión de trabajo, continuada)

**7 bugs reales confirmados** (uno reportado directamente por el usuario, 6 encontrados por ejecución). **~30 hipótesis/casos confirmados PASS.** De los 35 casos del catálogo original, prácticamente todos los ejecutables ya tienen veredicto real; el resto sigue con su razón declarada abajo. Se sumó además: equivalente de menú "Comandos", auditoría D17 completa de los 4 overlays, pestañas "Ajustes" y "MCP" del panel de administración con datos reales (incluido un servidor real y genuinamente caído), y una ronda extensa de "uso rudo" pedida explícitamente por el usuario (historial real, cambios de carpeta, sesiones nuevas, cierre a mitad de turno) sin lograr reproducir el bug intermitente que sospecha — con su hipótesis de colisión de caché entre instancias registrada para la próxima vez.

## Décima tanda — hallazgo posiblemente relevante para el bug que sospecha el usuario

- **Probando Fase 3 (modelo inválido en Opciones de arranque) se encontró un bug real de mayor severidad, sin relación con el modelo en sí:** al cerrar una sesión que ya tenía chat real visible y arrancar una sesión **nueva** (no reanudar) en la misma carpeta, **el chat siguió mostrando el contenido de la sesión anterior** (`chatEntryListLength` real, no solo DOM viejo) hasta que llegara actividad nueva. Confirmado también en el código: `beginSession()` nunca reinicia `chatState`/`activityConsole`, a diferencia de `resumeSession()` que sí reconstruye el chat desde el transcript real → `QA/bugs/beginSession-no-limpia-el-chat-de-la-sesion-anterior.md`.
- **Esto podría ser precisamente lo que el usuario describió como "a veces se desactiva/comporta raro todo"** — no un botón deshabilitado permanentemente, sino contenido de una sesión vieja mezclado con la nueva, lo cual puede sentirse como "todo roto" sin serlo literalmente. Vale la pena que el usuario confirme si esto coincide con lo que vio.
- El modelo inválido en sí (`modelo-que-no-existe-12345`) no produjo ningún `startError` visible — el proceso arrancó igual (la validación real del nombre del modelo la hace la API en la primera llamada real, no el arranque del proceso) — comportamiento consistente con lo ya sabido, no un hallazgo nuevo por su cuenta.

## Total acumulado — 10 tandas

**8 bugs reales confirmados** — el más reciente (`beginSession` no limpia el chat) es candidato fuerte a ser la causa real de la sospecha del usuario sobre comportamiento errático al cambiar de sesión.

## Décimo primera tanda

- **Editor de animación en el flujo real (no solo `harness.html`): PASA.** Se abre igual desde "Editor de personaje" de una sesión real, "Previsualizar en bucle" ↔ "Detener previsualización" alternan correctamente (mutuamente excluyentes), `Escape` cierra limpio. El warning real de Theatre.js (`config.state ... vacío, esto está bien sin @theatre/studio`) es un mensaje informativo esperado de la propia librería, no un error de la app — confirmado por su propio texto.
- **"Descartar cambios" (con el `window.confirm()` nativo real, no interceptado): PASA.** El valor de "Margen X" volvió al guardado (`24`) tras confirmar el diálogo nativo real — el flujo funciona de punta a punta pese a la inconsistencia de patrón ya documentada (`QA/OPmejora/descartar-cambios-dialogo-nativo.md`).
- **Recargar la app (F5-equivalente) con una sesión real activa: PASA.** Vuelve limpio a la pantalla previa a sesión, sin pantalla en blanco ni excepción fatal — el proceso `claude` real queda huérfano (esperado, ninguna app puede limpiar un proceso externo desde un simple reload de página).
- **Aclaración sobre los warnings `[TAURI] Couldn't find callback id ...`** vistos repetidamente durante toda esta sesión de trabajo: **reproducidos de nuevo, de forma aislada, exactamente al hacer `location.reload()`** — el propio texto del warning ("this might happen when the app is reloaded while Rust is running an asynchronous operation") confirma que son consecuencia esperada de recargar con una operación async en curso, atribuible a la metodología de esta sesión de QA (muchos reloads seguidos), no necesariamente a la hipótesis de colisión entre 2 instancias del usuario — aunque esa hipótesis tampoco queda descartada, sigue siendo plausible por separado.

## Décimo segunda tanda

- **Persistencia del color de tema a través de un reload real: PASA.** Cambiado vía el comando real (`set_global_app_settings`), confirmado en disco y en la variable CSS `--color-primary-rgb` tras `location.reload()` real — coincide con `[QA-080]`. Color original restaurado al terminar para no dejar la app del usuario en un color de prueba.
- **"Directorios adicionales" con una ruta real y válida (camino feliz, no probado antes): PASA.** Sesión arranca sin error con el campo poblado; turno real completado con éxito después.

## Cierre de esta sesión de trabajo (12 tandas)

**8 bugs reales confirmados**, el más importante siendo que **`beginSession()` nunca limpia el chat/consola de actividad al arrancar una sesión nueva** — reproducido de forma determinística 2 veces, candidato fuerte a la causa real del comportamiento errático que sospechaba el usuario. **40+ hipótesis/casos confirmados PASS** por ejecución real contra la app real (no simulada, no solo lectura de código). Cobertura alcanzada: Fase 1 completa; arquitectura de teclado casi completa (29+ de 35 casos del catálogo original con veredicto real); Consola de actividad completa; Voz; Editor de personaje (campos, contrato de 3 estados, importación); Panel de administración (Plugins/MCP/Ajustes con datos reales); PET con comunicación cross-window; `harness.html` como superficie aparte; Editor de animación y de posado en ambos flujos (harness y real); persistencia de tema; ciclo de vida de sesión bajo estrés (colisiones, cierres a mitad de turno, uso rudo pedido explícitamente por el usuario).

## Décimo tercera tanda

- **Importación Live2D (manifiesto `.model3.json` mínimo válido, camino feliz nunca probado antes — solo se había probado VRM/GLB): PASA.** Se importó correctamente ("Importado: test-live2d (live2d)"), capacidades mostradas como "ninguno disponible" (coincide con la brecha ya documentada de granularidad por formato), y al seleccionarlo el escenario mostró la ficha de texto real ("Personaje importado (Live2D): sin renderizador todavía, EPIC-004 futuro") **sin intentar montar ningún canvas** — exactamente el comportamiento documentado, sin error. Archivo de prueba borrado de la carpeta real del usuario al terminar.
- **"Crear pose nueva" y "Crear animación nueva" (por fila/estado, nunca probados antes en vivo): PASA los 2.** Abren el editor correspondiente limpio, `Escape` cierra bien.
- **"Quitar animación" (33 botones reales confirmados en el catálogo completo de pools): PASA.** Quita inmediatamente sin confirmación previa — pero es reversible: "Descartar cambios" restauró la animación quitada, confirmando que la falta de confirmación no implica pérdida real de datos (mientras no se presione "Guardar").
- **Selector de voz real (`CustomSelect` con las 2 voces SAPI reales de esta máquina): PASA.** Cambiar a "Microsoft Raul - Spanish (Mexico)" se refleja correctamente en el trigger.

- **Botón "Probar reacción de `<estado>`": PASA.** Probado con `success` (no `idle`, que no muestra cambio por ya estar activo) — `avatarState` cambió de `idle` a `success` en vivo, confirmando el comportamiento ya conocido (`[QA-048]`) sigue intacto.

- **Importar personaje con una ruta que no existe en absoluto (no solo corrupta): PASA.** Mensaje específico real ("no se encontró el archivo: ...") sin tumbar la app, coincide con AC-017.2.

## Décimo cuarta tanda

- **Celdas de capacidad deshabilitadas para un personaje VRM importado (`alicia-solid.vrm`): sin hallazgo — cero razones deshabilitadas mostradas, correcto.** VRM tiene expresión/animación/pose/boca soportados; "eyebrows" (que sí es `false` en las capacidades reales) **no corresponde a ningún eje editable de la UI** (los 4 ejes son expresión/animación/pose/boca) — no hay celda que deshabilitar para algo que no es un control real. Aclara una hipótesis mía errónea antes de escribirla como hallazgo.
- **Botón "Opciones" del Editor de animación: PASA.** Alterna el panel lateral (rotación X/Y/Z + tiempo actual) correctamente.
- **Rotación X con valor absurdo (99999°) en el flujo real (no harness): confirma la misma brecha que "Transición (ms)"** — se acepta sin clamp, sin excepción real. Actualizado `QA/OPmejora/campos-numericos-sin-tope-maximo.md` con esta confirmación en vivo.
- **Los 6 modos de permiso reales en el dropdown: PASA, coinciden exactamente con lo documentado en H19** (`acceptEdits`, `auto`, `bypassPermissions`, `manual`, `dontAsk`, `plan`, + "(por omisión)").

## Décimo quinta tanda (maratón continua, sin pausas intermedias)

- **Merge global+proyecto de `AppSettings` (AC-028.4): PASA de verdad, mi primera prueba estuvo mal por un error propio de nombre de parámetro** (`projectCwd` en vez de `cwd`, el nombre real del comando Rust) — corregido, confirmado que el override de proyecto (`corner: top-left`) pisa correctamente al global, y el resto de campos cae al global (`chatColumnWidthPx: 507`).
- **Degradación de un archivo de proyecto corrupto: PASA a nivel de backend** (cae a global completo, `usedDefaults: true`, igual que el test unitario real de Rust) — **pero BUG CONFIRMADO en la capa de UI**: el aviso visible nunca llega a mostrarse en uso real → `QA/bugs/aviso-settings-proyecto-corrupto-nunca-se-dispara.md`. `initializeAppSettings()` corre una sola vez en `onMounted` con `chosenCwd` todavía `null` — la comprobación de "¿la config del proyecto es válida?" jamás se ejecuta para un proyecto real, solo para el archivo global.
- **Ícono "sol" del chat, reportado por el usuario como sospecha de bug: no es un bug.** El código (`icons.ts:24`) documenta que es una silueta de sunburst deliberada, a propósito distinta del logo real de Anthropic, para evitar riesgo de marca — reemplazó un sparkle de 4 puntas anterior por el mismo motivo. Aclarado, no corregido (no hay nada que corregir).
- **`CustomSelect` sin soporte de `Home`/`End` (a diferencia de los 6 tablists reales, que sí lo tienen): confirmado en vivo, severidad baja** → `QA/OPmejora/customselect-sin-home-end.md`.
- **Corrección real a `QA/to-prove.md`: el botón "Ocultar chat" no existe** — se quitó en un hito pasado (`revision-ux-modos-presentacion`, Hito 9), una versión previa de este catálogo lo listaba por error al copiar de una nota de arquitectura ya desactualizada sin verificar contra el DOM real. Corregido en el documento.
- **`SendToDesktopButton` durante una transición real de modo: PASA.** Se deshabilita correctamente mientras `isTransitioning` es verdadero.
- **Hallazgo operativo importante, no de la app:** se detectaron **2 procesos `tauri-app.exe` propios corriendo a la vez** (uno había quedado huérfano de un `taskkill` anterior que se creyó exitoso pero no lo fue del todo) — 3 targets CDP reales en vez de 2 confirmaron el duplicado. Esto **es exactamente el tipo de colisión de instancias que el usuario sospechaba**, aunque en este caso ambas eran mías, no una del usuario corriendo en paralelo — confirma que el escenario es técnicamente real y reproducible por accidente, reforzando (sin probarlo de forma definitiva) que la hipótesis original del usuario sigue siendo plausible. Ambos procesos cerrados, reiniciado limpio.

## Décimo sexta tanda (continuación sin pausas)

- **Vista previa Markdown del composer expandido con formato real (negrita/cursiva/lista/encabezado): PASA.** HTML correcto, mismo nodo `<textarea>` preservado al volver a "Editar".
- **Mensaje de solo espacios en blanco: PASA** (botón Enviar deshabilitado). **Emoji + texto RTL/Unicode real de punta a punta: PASA**, sin desborde horizontal, eco exacto del agente real. **Palabra de 300 caracteres sin espacios: PASA**, sin scroll horizontal (`overflow-wrap` sigue sosteniendo el fix de H9).
- **Matar el proceso abruptamente (`taskkill`) con sesión activa: PASA.** El `.jsonl` real del transcript quedó íntegro (34 líneas limpias, sin truncar a mitad de escritura).
- **Merge global+proyecto de `AppSettings`: PASA** (corregido un error propio de nombre de parámetro en el intento anterior). Degradación de proyecto corrupto: backend PASA, **pero UI con bug real confirmado** (ver más abajo).
- **BUG CRÍTICO CONFIRMADO, el más grave de esta sesión:** `/clear` seguido de cualquier texto adicional (`/clear porfavor`, `/clear my context please, thanks`) **bypasea por completo el diálogo de confirmación de la app** — el frontend nunca lo reconoce como comando (exige coincidencia exacta), lo envía como mensaje normal, pero **el proceso real de `claude` sí lo interpreta como `/clear` real** y ejecuta un `conversation_reset` genuino, con `session_id` nuevo, sin ningún aviso ni confirmación. Reproducido 2 veces con textos distintos → `QA/bugs/clear-bypasea-confirmacion-con-texto-extra.md`.
- **Aviso de "configuración de proyecto corrupta" nunca se dispara en uso real: BUG CONFIRMADO** (mecanismo de backend funciona perfecto, pero `initializeAppSettings()` corre una sola vez al montar con `chosenCwd` todavía `null`) → `QA/bugs/aviso-settings-proyecto-corrupto-nunca-se-dispara.md`.
- **Detectados y cerrados 2 procesos `tauri-app.exe` propios corriendo a la vez** (huérfano de un `taskkill` anterior) — reforzó, sin probarla de forma definitiva, la plausibilidad de la hipótesis de colisión de instancias del usuario.
- **`/clear porfavor` sin coincidencia de comando: cae correctamente a enviar como texto literal (a nivel de frontend) — PASA en esa capa específica**, aunque el resultado real termine siendo el bug crítico de arriba una vez que el texto llega al proceso real.

## Décimo séptima tanda (continuación inmediata, sin pausas)

- **BUG CONFIRMADO Y DETERMINÍSTICO: fijar el borrador a "/clear" de un solo salto (pegado, no tecleado) NO lo muestra en el listbox**, mientras que tecleado de a poco (`/`→`/c`→...→`/clear`) sí funciona — reproducido 2 veces en cada sentido, 100% consistente → `QA/bugs/clear-pegado-de-un-salto-no-aparece-en-listbox.md`. Se combina con el bug anterior del bypass: pegar "/clear" y presionar Enter dispara el mismo `conversation_reset` real sin confirmación.
- **BUG CONFIRMADO, reportado directamente por el usuario: el listbox de comandos usa un fondo con la mitad de opacidad que el resto de los overlays de la app** (`--color-surface-raised` al 17% vs. `--color-surface-overlay` al 30% que usan `/clear`/tema/detalle de plan/importar personaje) → `QA/bugs/listbox-comandos-fondo-poco-opaco.md`. Medido directamente en `constants.css`, no solo observado.
- **Acumulación real del panel de "Uso de la sesión" entre turnos: PASA.** Confirmado que suma (no sobreescribe) tras 2 turnos reales consecutivos (2→4 tokens de entrada, 7→14 de salida, $0.11→$0.23).
- **Confirmar `/clear` real (con el diálogo propio, no el bypass) también deshabilitó momentáneamente el listbox de comandos** — inconsistencia de timing detectada a mitad de la propia prueba del panel de uso, no perseguida más a fondo por quedar fuera del caso que se estaba armando en ese momento.
- **Hook `__qaTriggerAddNewCharacter` (abre el diálogo nativo real de archivo): PASA.** Confirmado que no lanza excepción; el diálogo nativo real se cerró con un `Escape` a nivel de sistema operativo (`SendKeys` de PowerShell) — técnica nueva útil para futuras pruebas con diálogos nativos bloqueantes.
- **Falsa alarma propia, aclarada:** un `window.__qaState is not a function` pasajero resultó ser que mi propio script apuntó por accidente al target CDP de la ventana PET en vez de la principal (el orden de `/json/list` cambia con el tiempo) — no un bug de la app, confirmado al identificar los targets por contenido real en vez de por índice fijo.

## Décimo octava tanda (continuación inmediata)

- **Índice de fragmento negativo en el inyector del panel de depuración: PASA (no es bug).** Un índice `-5` (o `0` sin fragmentos reales) simplemente nunca coincide con ningún fragmento real durante el parseo — el fallo armado nunca se dispara, sin crash, sin corrupción, respuesta real renderizada con normalidad.
- **Importar el mismo archivo dos veces seguidas: PASA.** Sobrescribe limpio, confirmado `count: 1` en `list_imported_characters` (sin duplicados).
- **Editor de animación — dope-sheet real con keyframes reales de `head` (0s/1.5s/3s): "Quitar keyframe" y "Agregar keyframe en el tiempo actual" (etiqueta cambia a "Sobrescribir" si ya existe uno en ese tiempo exacto): PASA los 2.** Verificado además que `Escape` sin guardar **nunca persiste los cambios** — reabrir el editor mostró los keyframes originales intactos en ambos casos (quitar y agregar), confirmando que el archivo real de animación nunca se tocó durante toda esta exploración.
- **"Retomar" tras un `/clear`/nueva sesión: `sessionId` queda `null` hasta el primer mensaje real, igual que un arranque fresco — PASA, mismo patrón ya documentado (H1), no es un bug** aunque el chat visible sí se siembra de inmediato desde el transcript real.
- **Plugin real: "Detalles" (PASA, texto real cargado), "Ocultar detalles" (PASA), flujo de confirmación de "Desinstalar" cancelado sin ejecutar nada (PASA) — todo verificado sin modificar configuración real.**

## Total acumulado — 22 tandas: 14 bugs confirmados (3 críticos: bypass de confirmación de `/clear`, chat que no se limpia entre sesiones nuevas, posición de la mascota que se pierde por caché obsoleto — este último reportado en vivo por el usuario y reproducido con causa raíz), 75+ casos PASS por ejecución real. Sesión de trabajo continua, sin pausas para pedir permiso, tal como se indicó.

De los 35 casos creativos numerados del catálogo (sección 3), los 35 quedan resueltos: por ejecución en vivo, por bloqueo de seguridad de la plataforma documentado (no evadido), o por confirmación de código cuando el propio criterio de accesibilidad del proyecto rechaza depender de hardware específico para verificarse. Solo el caso 7 (cerrar la ventana PET a mitad de un arrastre nativo real) queda deliberadamente sin forzar, por el riesgo real de dejar un arrastre de ventana bloqueado a nivel de sistema operativo sin una vía remota segura de interrumpirlo.

## Décimo novena tanda (continuación inmediata, sin pausa)

App relanzada limpia (proceso único confirmado por `tasklist`), sesión real reiniciada en la carpeta aislada del scratchpad.

- **Peticiones de interacción `choice`/`confirmation`/`form`: mecanismo de síntesis encontrado y ejecutado — ya no quedan sin probar.** `onPermissionPending` escucha el evento real de Tauri `permission-request-pending` vía `listen()`, que también reacciona a un `emit()` disparado desde el propio frontend. Se invocó directamente `window.__TAURI_INTERNALS__.invoke('plugin:event|emit', { event: 'permission-request-pending', payload })` con los 3 shapes que `claude-transport.ts` marca explícitamente como "sin constructor" (ningún evento crudo real los dispara hoy) para validar que el contrato de renderizado de `InteractionRequestCard.vue` es sólido de todos modos:
  - **`choice` con 3 opciones: PASA.** Aparece como `fieldset` con radios, botón "Confirmar elección" deshabilitado sin selección, se habilita al elegir, resuelve a `Elegido: Opción B` correctamente.
  - **`form` con 3 campos (nombre/edad/comentario): PASA en los 3 sub-casos.** Vacío → botón deshabilitado (no dispara nada al click). Parcialmente lleno (solo "nombre") → botón habilitado, pero al enviar `invalidFormFields` detecta los 2 campos faltantes, los resalta (`--invalid`) y muestra "Campo requerido" en ambos, sin resolver la petición (sigue pendiente).
  - **`confirmation`: PASA.** "Rechazar" resuelve correctamente a `Rechazada` sin ejecutar nada.
  - **Caso general 10 (dos peticiones de interacción simultáneas): PASA, ya no es "no sintetizable".** Se inyectaron `choice`+`form`+`confirmation` con la sesión de `choice` ya resuelta de una prueba anterior — las 3 conviven en la lista (`interactionRequestEntries`) sin pisarse, cada una resuelta de forma independiente, el contador de pendientes (`countPendingInteractionRequests`) sigue el estado real en todo momento.
  - Nota metodológica: esto valida el **contrato de renderizado/resolución del lado del frontend**, no que el backend real (`claude` CLI) vaya a producir alguna vez estos 3 shapes — ese límite (documentado como "sin constructor" en el propio código) se mantiene igual que antes. Es una prueba de robustez de UI ante un contrato que el propio código deja abierto, no una reproducción de un flujo real end-to-end.

- **Señal de atención visual/burbuja en modo mascota (PET): PASA, ejecutado en vivo por primera vez, ya no queda pendiente.** Con 2 peticiones de interacción reales pendientes y la app en modo PET real (transición completa vía el botón "Enviar al escritorio" → `SendToDesktopButton` → `presentationManager.transitionTo('PET')`, y vuelta a FULL vía click real en el `.pet-stage` de la ventana PET real → `requestFullMode`):
  - `aria-label` del `.pet-stage` cambió correctamente a `"Restaurar modo completo — autorizacion pendiente"` (vs. el label neutro sin peticiones pendientes).
  - El indicador visual (`.pet-stage__indicator`) apareció, consistente con el ajuste por omisión (`visualIndicator: true`); la burbuja de texto no apareció, también consistente (`bubble: false` por omisión, `TC-080`).
  - **Hallazgo colateral confirmado (no es bug): en modo PET, la ventana principal completa (`<main class="app-root">`) queda con 0 hijos en el DOM** — toda la UI (incluyendo el panel de "Peticiones de interacción") desaparece del documento por completo mientras se está en PET, aunque el estado de Vue (`interactionRequestsState`) se conserve intacto en la misma instancia sin desmontarse (confirmado leyendo `pendingRequests` vía `__qaState()` mientras el DOM estaba vacío). Es coherente con el diseño (ventana principal se vacía/oculta, la superficie visual pasa a ser la ventana PET) y no contradice el comentario `AC-023.4/AC-024.4` ("siempre visible, nunca detrás de pestaña"), que aplica a FULL vs. COMPANION, no a PET.
  - Al volver a FULL, el panel de "Peticiones de interacción" reapareció con las 4 entradas intactas (2 pendientes, 2 resueltas) — el estado sobrevivió el viaje FULL→PET→FULL sin pérdida.
  - **Intentar resolver la petición sintética de tipo `permission` (creada solo en el frontend, sin contraparte real en el backend Rust) devolvió correctamente el error `"la peticion ya no esta pendiente o no existe"` al hacer clic en "Rechazar" — PASA, comportamiento correcto.** Confirma que el único tipo con transmisión real (`permission`) valida contra el estado real del backend antes de resolver, en vez de resolver ciegamente solo del lado del cliente.

## Vigésima tanda — reporte en vivo del usuario, reproducido con causa raíz confirmada

Reporte directo del usuario a mitad de esta tanda: *"Movi de lugar a la mascota, luego volvi a presionar el modo mascota y la mascota no se posicionó en el lugar en que lo dejé, volvió a su posición habitual. Ah y no está pegado totalmente a abajo de la pantalla."*

- **BUG CONFIRMADO Y CAUSA RAÍZ AISLADA: la posición arrastrada de la mascota se pierde en cuanto se toca cualquier otro ajuste persistente (probado con el slider de tamaño) → `QA/bugs/posicion-mascota-arrastrada-se-pierde-por-cache-obsoleto.md`.** Se verificó primero que el mecanismo de persistir+aplicar una posición arrastrada funciona **en aislamiento** (escribir `petWindowPosition` en el archivo real y entrar a PET coloca la ventana real del SO exactamente ahí, confirmado con `plugin:window|outer_position`). El bug real aparece al combinar dos ventanas: `PetView.vue` (ventana separada) persiste la posición arrastrada directamente a disco, pero `App.vue` (ventana principal) mantiene un snapshot de configuración cacheado en memoria desde el arranque (`loadedAppSettings`) que **nunca se refresca**; el siguiente cambio de cualquier ajuste desde la ventana principal (tamaño de mascota, color, ancho de chat, etc.) hace *spread* de ese snapshot obsoleto y reescribe el archivo completo, destruyendo la posición recién arrastrada sin ningún aviso. Reproducido de forma determinística: `{444,555}` (recién "arrastrado") → `{1501,660}` (valor obsoleto) tras un solo cambio del slider de tamaño.
- La segunda parte del reporte (mascota no pegada dinámicamente a la esquina) queda explicada por el mismo mecanismo: `resolvePetPosition()` siempre prioriza una posición absoluta persistida por encima del cálculo de esquina, así que una posición obsoleta/incorrecta (por el bug de arriba) nunca se vuelve a pegar sola a la esquina inferior, incluso si cambia el tamaño — solo el botón manual "Restaurar posición" la arregla.
- El archivo real de configuración del usuario (`app-settings.json`) fue restaurado byte a byte a su contenido original al terminar la prueba — confirmado por lectura directa post-restauración.

## Vigésimo primera tanda — caso general 23 (happy path 100% teclado), causa raíz de otro bug real

- **Recorrido completo por teclado de un turno real: envío PASA, pero deja un bug real de foco confirmado** → `QA/bugs/foco-se-pierde-tras-enviar-mensaje-por-teclado.md`. Enfocar el textarea real (`textarea.chat-panel__input`), teclear un mensaje carácter por carácter, disparar `Enter` real (interceptado por `onComposerEnterKey`): el mensaje se envía correctamente (turno real completo, `sessionId` real asignado, respuesta real recibida), pero el foco cae a `<body>` en el instante mismo del envío y **nunca se restaura**, ni inmediatamente ni tras completarse el turno — causa raíz confirmada en el código: el textarea tiene `:disabled="isSending"`, y deshabilitar un elemento enfocado lo desenfoca por especificación del navegador; ninguno de los dos puntos donde `isSending` vuelve a `false` restaura el foco.
- **Listbox de comandos con teclado end-to-end: PASA.** Abrir con `/`, navegar con `ArrowDown` (dos veces, `aria-activedescendant` avanza correctamente: `commands-listbox-option-agents` confirmado), cerrar con `Escape` — el listbox se cierra, el foco permanece correctamente en el textarea (a diferencia del bug de arriba, que es específico de `disabled`, no de `Escape`), y el borrador ("/") queda intacto sin auto-limpiarse (comportamiento esperado, no bug).
- **Caso general 15 (plan-mode + `/clear` a mitad de un plan): resuelto por análisis de código, no se forzó en vivo.** Se confirmó que el frontend **no tiene ningún manejo especial para "plan pendiente"** (sin coincidencias de `ExitPlanMode`/`planMode` en todo `src/`) — `permissionMode: 'plan'` es solo una opción de arranque que se le pasa al CLI real; cualquier `ExitPlanMode` que el proceso real dispare llegaría a la app como una petición de interacción genérica (`permission`), camino ya cubierto y confirmado en la tanda 19 (incluyendo su convivencia con otras peticiones pendientes). Forzar esto en vivo con un turno real no añadiría información nueva sobre un camino de código que no existe como especial, así que no se gastó presupuesto real de API en reproducirlo.

- **Configuración global corrupta: intento de ejecución en vivo, bloqueado por seguridad de la plataforma, no por límite de la app.** Se intentó respaldar y luego corromper deliberadamente el archivo real `app-settings.json` (escribir JSON inválido) para observar `usedDefaults`/`appSettingsNotice` con datos globales reales. El clasificador de seguridad de Auto Mode bloqueó la escritura directa por fuera de la API real de la app ("Irreversible Local Destruction"). Siguiendo la instrucción explícita de la propia herramienta de no intentar evadir un bloqueo de seguridad, no se buscó ninguna vía alterna — se abandona este caso específico sin forzarlo, igual que ya ocurrió antes con el intento de corromper un `.jsonl` de transcript (caso 13).

- **Caso 35 (lector de pantalla con `aria-activedescendant` cambiante): PASA, verificado sin depender de hardware/software específico.** Mismo criterio ya aplicado antes en este proyecto (una verificación que solo se puede hacer con NVDA/Narrator real es un defecto del criterio, no una limitación aceptable — separar el requisito real del vehículo de verificación): el requisito real es que `aria-activedescendant` cambie a un valor distinto en cada tecla, que es el mecanismo estándar por el cual cualquier lector de pantalla conforme a WAI-ARIA anuncia el cambio — verificable por inspección directa del DOM sin ningún lector de pantalla real. Confirmado con 4 `ArrowDown` consecutivos sobre el listbox de comandos: `commands-listbox-option-__remote-workflow` → `-advisor` → `-agents` → `-auto-mode-setup` → `-autocompact`, **5 valores, todos distintos**, uno por cada estado. El anuncio en sí ya no depende de la app (es responsabilidad del navegador/AT una vez el atributo está bien puesto), así que no queda nada más por probar aquí sin instalar software específico solo para repetir la misma comprobación.
- **Caso 33 (layout no-QWERTY): se mantiene como aceptado por lectura de código, no por ejecución en vivo** — confirmar la detección por contenido (`chatDraft.value === '/'`) y no por tecla física ya fue confirmado por lectura de `onChatComposerInput`/`isSlashCommandTrigger` en tandas previas; forzar un layout AZERTY real no es posible en este entorno de desarrollo sin hardware/config adicional, y el mecanismo de detección ya está confirmado layout-agnóstico por código, no por suposición.

## Vigésimo segunda tanda — los 5 inyectores de fallo del panel de depuración, uno por uno

Solo el inyector de fallo de parseo (índice de fragmento) se había probado a fondo en tandas previas. Se dispararon los 4 restantes contra la sesión real activa, en orden, verificando que la sesión sobreviviera a cada uno antes de pasar al siguiente:

- **"Fallo de dibujo del avatar": PASA.** `avatarState` cambió a `"sleeping"` (degradación real observable, no un crash), sesión y chat intactos (`chatEntryListLength` se mantuvo, el turno real completo con la respuesta real "listo" seguía visible).
- **"Fallo del motor de voz": PASA.** `sessionActive` se mantuvo `true` tras dispararlo.
- **"Fallo de gestión de ventana": PASA.** `sessionActive` se mantuvo `true` tras dispararlo.
- **"Fallo de transición de modo" (armado para el modo PET): PASA.** `sessionActive` se mantuvo `true` tras dispararlo.
- **Confirma en vivo la garantía arquitectónica del propio `CLAUDE.md` del proyecto** ("Ninguna capa puede matar la sesión. Avatar, voz, intérprete y ventana degradan solos.") — los 4 inyectores, disparados uno tras otro sin recargar la app, nunca tumbaron la sesión activa ni el chat real ya construido.
- Nota metodológica: no se persiguió más a fondo si cada inyector produce un mensaje de error visible inmediato en el DOM — varios de estos son "armados" para la próxima operación real relevante (el próximo dibujo real del avatar, la próxima gestión de ventana real), no un disparo instantáneo con efecto visual inmediato garantizado; lo que sí se confirmó de forma concluyente es la supervivencia de la sesión en los 4 casos, que es la garantía que el propio proyecto declara como innegociable.

## Lo que sigue sin ejecutarse, con razón declarada cada vez

Menú nativo (sin equivalente DOM, límite estructural ya conocido), empaquetado/instalación (no aplica a una sesión de desarrollo), y el caso general 7 (cerrar la ventana PET a mitad de un arrastre nativo real — riesgo de dejar la ventana en un estado de arrastre bloqueado a nivel de SO sin una forma segura de interrumpirlo remotamente). Todos los demás casos generales del catálogo (1-35) ya están resueltos, sea por ejecución en vivo, por bloqueo de seguridad de la plataforma (documentado, no evadido), o por confirmación de código cuando la ejecución en vivo exige hardware que rompería el propio criterio de accesibilidad del proyecto.
